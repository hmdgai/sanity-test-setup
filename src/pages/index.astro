---
/**
 * index.astro — HMDG Base Template Design System Reference
 * ─────────────────────────────────────────────────────────
 * Internal documentation only. Not a client-facing page.
 * Every design token, component, and pattern is documented here.
 *
 * RULE: Only global CSS classes and Tailwind tokens — no new styles on this page.
 */
import "../styles/global.css";
import CookieConsent from "../components/CookieConsent.astro";
import ContactForm  from "../components/ContactForm.astro";

const colours = [
  { label: "--color-primary",       hex: "#DB003E", desc: "Main brand colour — buttons, links, accents. Change per client.",              group: "Brand"   },
  { label: "--color-primary-hover", hex: "#DD0D48", desc: "Hover state for all primary interactive elements.",                             group: "Brand"   },
  { label: "--color-accent",        hex: "#FDF2F5", desc: "Light brand tint — section backgrounds and secondary fills.",                   group: "Brand"   },
  { label: "--color-headline",      hex: "#040303", desc: "Near-black — applied to h1–h6 automatically via @layer base.",                  group: "Content" },
  { label: "--color-body",          hex: "#151515", desc: "Near-black — paragraphs and all body text.",                                    group: "Content" },
  { label: "--color-caption",       hex: "#94A3B8", desc: "Slate grey — captions, labels, meta text, secondary copy.",                    group: "Content" },
  { label: "--color-muted",         hex: "#F2F2F2", desc: "Light grey — alternating section backgrounds.",                                 group: "Surface" },
  { label: "--color-surface",       hex: "#F8FAFC", desc: "Off-white — card and form backgrounds.",                                        group: "Surface" },
  { label: "--color-border",        hex: "#E2E8F0", desc: "Subtle border and divider colour.",                                             group: "Surface" },
  { label: "--color-white",         hex: "#FFFFFF", desc: "Pure white — card surfaces, nav, modals.",                                      group: "Surface" },
  { label: "--color-black",         hex: "#000000", desc: "Pure black — use sparingly.",                                                   group: "Surface" },
  { label: "--color-hero-dark",     hex: "#0a0f1a", desc: "Deep navy-black — hero and dark footer backgrounds on legal and index pages.", group: "Surface" },
];

const buttons = [
  { cls: "btn-default",     dark: false, desc: "Primary filled. Main CTAs, hero sections, form submits. Always on a light background." },
  { cls: "btn-secondary",   dark: false, desc: "Outlined primary. Secondary actions next to btn-default. Light backgrounds only." },
  { cls: "btn-white",       dark: true,  desc: "White filled. Use on dark or brand-coloured backgrounds only." },
  { cls: "btn-transparent", dark: true,  desc: "Ghost white outline. Use on image overlays or dark sections only." },
];

const headings = [
  { tag: "h1", label: "H1", size: "clamp(2.5rem → 4.5rem)",     weight: "800", lh: "1.08", use: "Hero headlines, page titles"      },
  { tag: "h2", label: "H2", size: "clamp(2rem → 3.5rem)",       weight: "800", lh: "1.12", use: "Section headings"                 },
  { tag: "h3", label: "H3", size: "clamp(1.5rem → 2.5rem)",     weight: "700", lh: "1.2",  use: "Sub-sections, card titles"       },
  { tag: "h4", label: "H4", size: "clamp(1.25rem → 1.75rem)",   weight: "700", lh: "1.3",  use: "Feature names, labels"           },
  { tag: "h5", label: "H5", size: "clamp(1.125rem → 1.375rem)", weight: "600", lh: "1.35", use: "Eyebrow labels, small headings"  },
  { tag: "h6", label: "H6", size: "clamp(1rem → 1.125rem)",     weight: "600", lh: "1.4",  use: "Fine labels, footnote headings"  },
];

const textStyles = [
  { token: "--text-base", px: "16px", lh: "1.75", cls: "",        label: "Base",  desc: "Default body text",              sample: "This is standard body text at 16px. Line-height 1.75 for comfortable reading. Clinic websites serve patients of all ages including older adults, so readability is always the priority. Never reduce body text below 16px." },
  { token: "--text-lg",   px: "18px", lh: "1.8",  cls: "text-lg", label: "Large", desc: "Lead paragraphs, intros",       sample: "This is a lead paragraph at 18px. Use for the first paragraph in a section or anywhere you need more visual weight without a heading. Apply class .section-lead or text-lg." },
  { token: "--text-sm",   px: "15px", lh: "1.6",  cls: "text-sm", label: "Small", desc: "Captions, labels, metadata",   sample: "Small text at 15px for captions, metadata, certifications, and supporting information. Use for dates, addresses, and non-critical copy." },
];

const features = [
  { title: "HCPC Registered",   desc: "All clinicians are fully registered and hold current professional indemnity insurance." },
  { title: "4.9 Google Rating", desc: "Over 500 verified patient reviews. Trusted by the local community for over a decade." },
  { title: "Fast Results",      desc: "Most patients see measurable improvement within 2–3 sessions. No long waiting lists." },
  { title: "Personal Approach", desc: "Every treatment plan is built around your specific goals, lifestyle, and recovery needs." },
];

const navLinks = [
  { href: "#workflow",   label: "Workflow",       num: "01" },
  { href: "#colours",    label: "Colours",        num: "02" },
  { href: "#fonts",      label: "Fonts",          num: "03" },
  { href: "#typography", label: "Typography",     num: "04" },
  { href: "#body-text",  label: "Body Text",      num: "05" },
  { href: "#buttons",    label: "Buttons",        num: "06" },
  { href: "#forms",      label: "Forms",          num: "07" },
  { href: "#containers", label: "Containers",     num: "08" },
  { href: "#flex",       label: "Flex Layout",    num: "09" },
  { href: "#cards",      label: "Cards",          num: "10" },
  { href: "#combined",   label: "Example",        num: "11" },
  { href: "#utilities",  label: "Utility Classes",num: "12" },
  { href: "#tokens",     label: "Design Tokens",  num: "13" },
  { href: "#globalcss",  label: "global.css",     num: "14" },
  { href: "#images",     label: "Image Rules",    num: "15" },
  { href: "#security",   label: "Security",       num: "16" },
  { href: "#agents",     label: "Agent System",   num: "17" },
  { href: "#modes",      label: "Modes & Tiers",  num: "18" },
  { href: "#skills",     label: "Skills Library", num: "19" },
  { href: "#perfbuild",  label: "Perf & Build",   num: "20" },
  { href: "#herovideo",  label: "Hero Video",     num: "21" },
  { href: "#projectlog", label: "Project Log",    num: "22" },
  { href: "#envvars",    label: "Env Variables",  num: "23" },
  { href: "#baselayout", label: "BaseLayout",     num: "24" },
  { href: "#cookiecfg",  label: "Cookie Config",  num: "25" },
  { href: "#carousel",   label: "Carousels",      num: "26" },
  { href: "#reviews",    label: "Google Reviews", num: "27" },
  { href: "#setup",      label: "Client Setup",   num: "28" },
  { href: "#deploy",     label: "Deploy to CF",   num: "29" },
  { href: "#fallback",   label: "Fallback Redirect", num: "30" },
];
---

<!doctype html>
<html lang="en-GB">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Design System — HMDG Base Template</title>
    <style>
      /* Docs page — headings render at editorial weight 400, not landing page weight */
      h1, h2, h3, h4, h5, h6 { font-weight: 400; }

      /* Typography specimen — restore natural weights inside preview wrapper */
      .type-preview h1, .type-preview h2 { font-weight: var(--font-weight-extrabold); }
      .type-preview h3, .type-preview h4 { font-weight: var(--font-weight-bold); }
      .type-preview h5, .type-preview h6 { font-weight: var(--font-weight-semibold); }

      /* Design token radius demos — border-radius set via JS CSS variable */
      .radius-demo {
        border-radius: var(--demo-radius, 0px);
      }

      /* Sidebar active link state — toggled by IntersectionObserver */
      .sidebar-link.is-active {
        color: var(--color-headline);
        background-color: var(--color-accent);
        font-weight: var(--font-weight-semibold);
      }
      .sidebar-link.is-active .sidebar-num {
        color: var(--color-headline);
      }

      /* Dark sections — hero and footer use deep navy-black token */
      .ds-dark {
        background-color: var(--color-hero-dark);
      }
    </style>
  </head>

  <body class="bg-[--color-muted] text-[--color-body]">


    <!-- ══════════════════════════════════════════════════════
         STICKY HEADER
    ══════════════════════════════════════════════════════ -->


    <!-- ══════════════════════════════════════════════════════
         TWO-COLUMN LAYOUT: Sidebar + Main
    ══════════════════════════════════════════════════════ -->
    <div class="lg:flex">


      <!-- ── LEFT SIDEBAR ──────────────────────────────────── -->
      <aside class="hidden lg:flex flex-col w-64 xl:w-72 shrink-0 sticky top-0 h-screen bg-white border-r border-[--color-border] overflow-y-auto">

        <nav class="px-4 py-6 flex-1" aria-label="Design system sections">
          <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest px-3 mb-4">Sections</p>
          <ul class="flex flex-col gap-0.5 list-none p-0 m-0">
            {navLinks.map(link => (
              <li>
                <a
                  href={link.href}
                  data-nav={link.href.slice(1)}
                  class="sidebar-link flex items-center gap-3 px-3 py-2.5 rounded-[--radius-btn] text-sm text-[--color-body] hover:text-[--color-headline] hover:bg-[--color-accent] transition-all group"
                >
                  <span class="sidebar-num text-xs font-mono font-bold text-[--color-caption] group-hover:text-[--color-headline] transition-colors w-5 shrink-0">{link.num}</span>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>

        <div class="px-4 py-5 border-t border-[--color-border]">
          <p class="text-xs text-[--color-caption] m-0">Internal use only</p>
          <p class="text-xs text-[--color-caption] m-0 mt-0.5">Do not distribute</p>
        </div>

      </aside>


      <!-- ── RIGHT: MAIN CONTENT ───────────────────────────── -->
      <div class="flex-1 min-w-0">


        <!-- HERO -->
        <section class="ds-dark">
          <div class="px-8 md:px-10 xl:px-14 py-16 max-w-4xl">

            <div class="flex flex-col gap-6">
              <span class="eyebrow">HMDG · Internal Documentation</span>
              <h1 class="text-white m-0">
                HMDG Base Template
              </h1>
              <p class="text-white/70 m-0 max-w-xl leading-relaxed">
                The global design system reference for all HMDG UK clinic websites.
                Every colour, font, heading, button, container, component, image rule, security standard, and agent workflow is documented here.
              </p>
              <div class="flex flex-wrap gap-2">
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">Astro 6.1</span>
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">Tailwind v4</span>
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">Cloudflare Pages</span>
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">12 AI Agents</span>
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">6 Skills</span>
                <span class="text-xs font-semibold text-white/50 bg-white/10 border border-white/10 px-3 py-1 rounded-full">v2.0.0</span>
              </div>
              <p class="text-white/50 text-sm m-0">
                Started: March 5 2026 &nbsp;·&nbsp; Last major update: April 2026
                &nbsp;·&nbsp; Do not rename any CSS variable or class unless adding new ones.
              </p>
              <div class="flex flex-wrap gap-3">
                <a href="#colours" class="btn btn-default">View Design System</a>
                <a href="#combined" class="btn btn-transparent">See Example</a>
              </div>
            </div>

            <!-- Mobile section nav pills -->
            <nav aria-label="Jump to section" class="flex flex-wrap gap-2 mt-10 pt-8 border-t border-white/10 lg:hidden">
              {navLinks.map(link => (
                <a
                  href={link.href}
                  class="text-xs font-semibold text-[--color-caption] bg-white/10 hover:bg-white/20 hover:text-[--color-caption] border border-white/10 px-3 py-1.5 rounded-[--radius-badge] transition-colors"
                >
                  {link.label}
                </a>
              ))}
            </nav>

          </div>
        </section>


        <!-- ARTICLES -->
        <main class="px-6 md:px-8 xl:px-12 py-8 flex flex-col gap-5 w-full">


          <!-- ───────────────────────────────────────────────────
               01 — WORKFLOW (Developer + Support team)
          ─────────────────────────────────────────────────────── -->
          <article id="workflow" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">01</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Workflow</h5>
                  <p class="text-sm text-[--color-caption] m-0">How developers and the support team work with this template. Read this before anything else.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Workflow</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-8">

              <!-- Plain English -->
              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Two groups of people use this template. <strong>Developers</strong> use it to build a new clinic website. <strong>Support team</strong> uses it to fix tickets on a website that is already live. Both groups go through GitHub. GitHub is the single source of truth — no one edits files directly on Cloudflare.
                </p>
              </div>

              <!-- ───────── WORKFLOW A — DEVELOPER ───────── -->
              <section class="flex flex-col gap-5">

                <header class="flex flex-wrap items-center gap-3">
                  <span class="text-[10px] font-bold text-white bg-[--color-primary] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wider">Workflow A</span>
                  <h5 class="m-0">Developers — building a new client site</h5>
                </header>

                <p class="m-0 text-sm text-[--color-body]">
                  Every website developer must follow these steps when starting a new clinic project. The base template is the source — every client site is a separate private repository created from a clone of this template. Never edit the base template directly for a client.
                </p>

                <!-- Numbered steps -->
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    { num: "01", title: "Clone the base template",      body: "Open a terminal on your local computer. Run: git clone https://github.com/felmerald-hmdg/astro-base-template-cloudflare.git PROJECT-NAME — replacing PROJECT-NAME with the client's name." },
                    { num: "02", title: "Customise locally",            body: "Open the cloned folder in your editor. Customise per the Client Setup Checklist (Section 27 — Phase 2). Run npm install, npm run dev, then test locally." },
                    { num: "03", title: "Create a NEW private repo on GitHub", body: 'Go to github.com → "New repository". Name it exactly the same as the client (PROJECT-NAME). Set it to PRIVATE. Do NOT initialise with a README — the cloned template already has one.' },
                    { num: "04", title: "Point the local clone at the new repo", body: "Back in the terminal, inside the cloned folder, run: git remote set-url origin https://github.com/YOUR-ORG/PROJECT-NAME.git — then git push -u origin main." },
                    { num: "05", title: "Deploy to Cloudflare Pages",   body: "Follow the step-by-step deployment guide in Section 28. The first deploy connects the new private repo to a Cloudflare Pages project. After that, every push to main auto-deploys." },
                  ].map(s => (
                    <li class="flex items-start gap-4 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="flex items-center justify-center w-9 h-9 shrink-0 rounded-full bg-[--color-primary] text-white font-bold text-xs font-mono">{s.num}</span>
                      <div class="flex-1 min-w-0">
                        <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                        <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                      </div>
                    </li>
                  ))}
                </ol>

                <!-- Diagram A -->
                <div class="border-t border-[--color-border] pt-5">
                  <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0 mb-4">Visual flow</p>
                  <div class="max-w-xl mx-auto flex flex-col items-stretch">

                    <!-- Box 1 -->
                    <div class="flex items-center gap-4 p-4 bg-white border-2 border-[--color-primary] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-accent] text-[--color-primary]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Source</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">HMDG Base Template</p>
                        <p class="m-0 text-xs text-[--color-body]">Public template repo on GitHub</p>
                      </div>
                    </div>

                    <!-- Arrow with label -->
                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">git clone</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 2 -->
                    <div class="flex items-center gap-4 p-4 bg-white border border-[--color-border] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
                          <line x1="8" y1="21" x2="16" y2="21"></line>
                          <line x1="12" y1="17" x2="12" y2="21"></line>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Step 2</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Developer's local computer</p>
                        <p class="m-0 text-xs text-[--color-body]">Customise per Client Setup Checklist (Section 27)</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">git push</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 3 -->
                    <div class="flex items-center gap-4 p-4 bg-white border border-[--color-border] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
                          <path d="M9 9h6v6H9z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Step 3</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">New PRIVATE GitHub repo</p>
                        <p class="m-0 text-xs text-[--color-body]">Named "PROJECT-NAME" (the client)</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">connect (Section 28)</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 4 - destination -->
                    <div class="flex items-center gap-4 p-4 bg-[--color-primary]/8 border-2 border-[--color-primary] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-primary] text-white">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">Live</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Cloudflare Pages</p>
                        <p class="m-0 text-xs text-[--color-body]">Auto-deploys on every push to main</p>
                      </div>
                    </div>

                  </div>
                </div>

              </section>


              <!-- ───────── WORKFLOW B — SUPPORT TEAM ───────── -->
              <section class="flex flex-col gap-5 border-t-2 border-[--color-border] pt-8">

                <header class="flex flex-wrap items-center gap-3">
                  <span class="text-[10px] font-bold text-white bg-[--color-headline] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wider">Workflow B</span>
                  <h5 class="m-0">Intercom support team — fixing tickets on a live site</h5>
                </header>

                <p class="m-0 text-sm text-[--color-body]">
                  Once a client site is live, ongoing support is handled by the Intercom team. Every support team member must have a GitHub account with access to the client's repository. Fixes are made on a feature branch, opened as a pull request, and merged to <code class="code-label">main</code> only by Fel. Cloudflare auto-deploys after the merge.
                </p>

                <!-- Numbered steps -->
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    { num: "01", title: "Receive the ticket in Intercom",       body: "A customer or staff member reports an issue or change request through Intercom. Read the ticket in full and identify which file(s) or component(s) need to change." },
                    { num: "02", title: "Pull the project locally",              body: "On your local computer, run git clone for the relevant client's GitHub repository (or git pull if you already have it locally). Run npm install, then npm run dev to verify everything still works before you start." },
                    { num: "03", title: "Make the fix on a feature branch",      body: "Create a new branch named after the ticket, e.g. git checkout -b fix/contact-form-typo. Edit the file(s), test in the browser, and confirm the change addresses the ticket exactly." },
                    { num: "04", title: "Push the branch to GitHub",             body: "Commit your changes with a clear message describing the fix. Push the branch: git push -u origin fix/contact-form-typo. Then open a Pull Request on GitHub from your branch into main." },
                    { num: "05", title: "Notify Fel for review",                 body: "Tell Fel the PR is ready (Slack, email, or comment on the PR itself). Include the Intercom ticket reference. Wait for review — DO NOT merge yourself." },
                    { num: "06", title: "Fel merges to main",                    body: "Fel reviews the PR, requests changes if needed, and merges the approved PR to main. This is the only human gate in the deploy chain." },
                    { num: "07", title: "Cloudflare auto-deploys",               body: "The merge to main triggers an automatic Cloudflare Pages build and deploy. Live within 2–4 minutes. Verify the fix on the production URL and update the Intercom ticket as resolved." },
                  ].map(s => (
                    <li class="flex items-start gap-4 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="flex items-center justify-center w-9 h-9 shrink-0 rounded-full bg-[--color-headline] text-white font-bold text-xs font-mono">{s.num}</span>
                      <div class="flex-1 min-w-0">
                        <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                        <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                      </div>
                    </li>
                  ))}
                </ol>

                <!-- Diagram B -->
                <div class="border-t border-[--color-border] pt-5">
                  <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0 mb-4">Visual flow</p>
                  <div class="max-w-xl mx-auto flex flex-col items-stretch">

                    <!-- Box 1 -->
                    <div class="flex items-center gap-4 p-4 bg-white border-2 border-[--color-headline] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Trigger</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Intercom ticket</p>
                        <p class="m-0 text-xs text-[--color-body]">Customer or staff reports an issue / change</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">git pull</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 2 -->
                    <div class="flex items-center gap-4 p-4 bg-white border border-[--color-border] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Step 2-3</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Local fix on a feature branch</p>
                        <p class="m-0 text-xs text-[--color-body]">Edit, test in browser, commit</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">git push + open PR</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 3 - PR -->
                    <div class="flex items-center gap-4 p-4 bg-white border border-[--color-border] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <circle cx="18" cy="18" r="3"></circle>
                          <circle cx="6" cy="6" r="3"></circle>
                          <path d="M13 6h3a2 2 0 0 1 2 2v7"></path>
                          <line x1="6" y1="9" x2="6" y2="21"></line>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Step 4</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Pull Request on GitHub</p>
                        <p class="m-0 text-xs text-[--color-body]">From feature branch → main</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-headline] text-white text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">notify Fel</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 4 - Human gate (highlighted) -->
                    <div class="flex items-center gap-4 p-4 bg-[--color-primary]/8 border-2 border-[--color-primary] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-primary] text-white">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M9 12l2 2 4-4"></path>
                          <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">Human gate</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Fel reviews + merges to main</p>
                        <p class="m-0 text-xs text-[--color-body]">The only manual approval in the chain</p>
                      </div>
                    </div>

                    <div class="flex flex-col items-center py-2">
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <span class="px-3 py-1 bg-[--color-accent] text-[--color-primary] text-[10px] font-mono font-bold uppercase tracking-wider rounded-[--radius-badge]">auto-deploy</span>
                      <div class="w-px h-4 bg-[--color-border]"></div>
                      <svg class="w-4 h-4 text-[--color-border]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <polyline points="6 9 12 15 18 9"></polyline>
                      </svg>
                    </div>

                    <!-- Box 5 - Live -->
                    <div class="flex items-center gap-4 p-4 bg-white border border-[--color-border] rounded-[--radius-card]">
                      <div class="flex items-center justify-center w-12 h-12 shrink-0 rounded-full bg-[--color-muted] text-[--color-headline]">
                        <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                          <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"></path>
                        </svg>
                      </div>
                      <div class="flex-1">
                        <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-wide">Live</p>
                        <p class="m-0 text-sm font-bold text-[--color-headline]">Cloudflare auto-deploys</p>
                        <p class="m-0 text-xs text-[--color-body]">Live in 2–4 min · update Intercom as resolved</p>
                      </div>
                    </div>

                  </div>
                </div>

              </section>


              <!-- ───────── ROLES & RESPONSIBILITIES ───────── -->
              <section class="flex flex-col gap-3 border-t-2 border-[--color-border] pt-8">
                <header class="flex items-center gap-3">
                  <h5 class="m-0">Roles &amp; responsibilities</h5>
                </header>
                <p class="m-0 text-sm text-[--color-body]">Quick reference: who is responsible for what step.</p>

                <div class="overflow-x-auto">
                  <table class="w-full border border-[--color-border] rounded-[--radius-card] overflow-hidden text-xs">
                    <thead class="bg-[--color-muted]">
                      <tr>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Role</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">GitHub access</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Can clone template?</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Can push to feature branch?</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Can merge to main?</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Owns deploy?</th>
                      </tr>
                    </thead>
                    <tbody>
                      {[
                        { role: "Developer",         gh: "Yes",        clone: "Yes — for new clients",       feat: "Yes",   merge: "No",  deploy: "Sets up first deploy in Cloudflare" },
                        { role: "Intercom support",  gh: "Yes",        clone: "Yes — to fix tickets",         feat: "Yes",   merge: "No",  deploy: "No (auto on merge)" },
                        { role: "Fel",                gh: "Admin",      clone: "Yes",                          feat: "Yes",   merge: "Yes — only person", deploy: "Owner of Cloudflare project" },
                        { role: "Marketing team",    gh: "No",         clone: "No",                           feat: "No",    merge: "No",  deploy: "Provides inputs (Section 27 Phase 1)" },
                        { role: "Client",             gh: "No",         clone: "No",                           feat: "No",    merge: "No",  deploy: "Provides content + signs off" },
                      ].map(r => (
                        <tr class="border-t border-[--color-border] bg-white">
                          <td class="px-3 py-2 align-top font-semibold text-[--color-headline]">{r.role}</td>
                          <td class="px-3 py-2 align-top text-[--color-body]">{r.gh}</td>
                          <td class="px-3 py-2 align-top text-[--color-body]">{r.clone}</td>
                          <td class="px-3 py-2 align-top text-[--color-body]">{r.feat}</td>
                          <td class="px-3 py-2 align-top text-[--color-body]">{r.merge}</td>
                          <td class="px-3 py-2 align-top text-[--color-body]">{r.deploy}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </section>


              <!-- ───────── GOLDEN RULES ───────── -->
              <section class="flex flex-col gap-3 border-t-2 border-[--color-border] pt-8">
                <header>
                  <h5 class="m-0">Golden rules — read once, follow always</h5>
                </header>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "GitHub is the single source of truth. Never edit files directly on Cloudflare.",
                    "Every client site is a SEPARATE private repo. The base template is never edited for one client.",
                    'Repository name = "PROJECT-NAME" (the client). Use the same string in Cloudflare Pages and the Web3Forms form name so everything stays consistent.',
                    "Only Fel merges to main. Everyone else opens pull requests.",
                    "Cloudflare auto-deploys on every merge to main. There is no manual deploy button to press.",
                    "If something goes wrong, revert the merge commit on GitHub. The previous deploy is restored automatically.",
                  ].map(r => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{r}</p>
                    </li>
                  ))}
                </ul>
              </section>

            </div>

            <!-- Claude prompts (one for each role) -->
            <div class="bg-white px-8 pb-8 flex flex-col gap-3">
              <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — Developer: start a new client build</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">I'm starting a new clinic site for [CLIENT NAME] using the HMDG base template. (1) Confirm I have all Phase 1 marketing inputs from Section 27. (2) Walk me through git clone of the base template, npm install, and dev verification. (3) Help me create a new private GitHub repo named exactly [CLIENT NAME], reset the local remote, and push. (4) Then guide me through the Cloudflare Pages deploy steps in Section 28 — including the four PUBLIC_* env vars under Build scope and Retry deployment. Confirm npm run build is clean before I push.</p>
                </div>
              </div>

              <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — Support: fix an Intercom ticket</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">I have an Intercom ticket for [CLIENT NAME]. Ticket details: [PASTE TICKET]. (1) Confirm I have the latest from origin/main with git pull. (2) Help me identify which file(s) need to change. (3) Create a feature branch named fix/[ticket-summary]. (4) Make the fix, test locally with npm run dev, run npm run build to verify. (5) Commit + push the branch. (6) Open a PR on GitHub from the branch into main. (7) Tell me what to write to Fel so they can review + merge. Do NOT merge yourself.</p>
                </div>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               02 — GLOBAL COLOUR VARIABLES
          ─────────────────────────────────────────────────────── -->
          <article id="colours" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">02</span>
                <h5 class="m-0">Global Colour Variables</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Colors</span>
            </header>

            <div class="px-8 py-6 grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">
              {colours.map(c => (
                <div class="flex flex-col gap-3 p-5 rounded-[--radius-card] bg-[--color-muted] border border-[--color-border]">
                  <div class="h-14 rounded-[--radius-btn]" data-hex={c.hex}></div>
                  <div class="flex flex-col gap-1">
                    <div class="flex items-center gap-2 flex-wrap">
                      <code class="code-label">{c.label}</code>
                      <code class="code-hex">{c.hex}</code>
                    </div>
                    <span class="text-xs font-semibold text-[--color-caption] uppercase tracking-wider">{c.group}</span>
                    <p class="text-xs text-[--color-body] m-0 mt-1 leading-snug">{c.desc}</p>
                  </div>
                </div>
              ))}
            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Colours</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Update the primary brand colour to [HEX]. Change --color-primary, --color-primary-hover (slightly darker), and --color-accent (very light tint of primary). These three variables are in src/styles/global.css inside the @theme block. Do not change any other colour variables.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               02 — FONT VARIABLES
          ─────────────────────────────────────────────────────── -->
          <article id="fonts" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">03</span>
                <h5 class="m-0">Font Variables</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Fonts</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-5">

              <div class="p-6 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex flex-col gap-4">
                <div class="flex items-start justify-between flex-wrap gap-3">
                  <div class="flex flex-col gap-1">
                    <code class="code-label">--font-heading</code>
                    <p class="text-sm text-[--color-body] m-0">Inter Tight · Condensed display variant · Used on all headings h1–h6</p>
                  </div>
                  <span class="text-xs font-semibold text-[--color-caption] bg-white border border-[--color-border] px-3 py-1 rounded-[--radius-badge] shrink-0">Heading Font</span>
                </div>
                <p class="font-heading text-5xl text-[--color-headline] m-0 leading-none tracking-tight">
                  Aa Bb Cc Dd — The quick brown fox
                </p>
                <p class="text-xs text-[--color-caption] m-0 font-mono">font-family: 'Inter Tight', ui-sans-serif, system-ui, sans-serif</p>
              </div>

              <div class="p-6 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex flex-col gap-4">
                <div class="flex items-start justify-between flex-wrap gap-3">
                  <div class="flex flex-col gap-1">
                    <code class="code-label">--font-body</code>
                    <p class="text-sm text-[--color-body] m-0">Inter · Standard reading variant · Used on all body text, inputs, and buttons</p>
                  </div>
                  <span class="text-xs font-semibold text-[--color-caption] bg-white border border-[--color-border] px-3 py-1 rounded-[--radius-badge] shrink-0">Body Font</span>
                </div>
                <p class="font-body text-2xl text-[--color-headline] m-0 leading-relaxed">
                  Aa Bb Cc Dd — The quick brown fox jumps over the lazy dog. 1234567890
                </p>
                <p class="text-xs text-[--color-caption] m-0 font-mono">font-family: 'Inter', ui-sans-serif, system-ui, sans-serif</p>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Fonts</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Update the heading font to [FONT NAME]. Change --font-heading in the @theme block of src/styles/global.css. Also update the Google Fonts import link in BaseLayout.astro and src/pages/index.astro to load the new font. Keep --font-body as Inter unless asked to change it.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               03 — TYPOGRAPHY SCALE
          ─────────────────────────────────────────────────────── -->
          <article id="typography" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">04</span>
                <h5 class="m-0">Typography Scale — H1 to H6</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Type</span>
            </header>

            <div class="px-8 pt-6">
              <pre class="code-block"><code>{"/* Applied globally via @layer base — no class needed */\nh1 { font-size: clamp(2.5rem,  3vw + 1.8rem,    4.5rem);  line-height: 1.08; letter-spacing: -0.03em;  font-weight: 800; }\nh2 { font-size: clamp(2rem,    2.25vw + 1.47rem, 3.5rem);  line-height: 1.12; letter-spacing: -0.025em; font-weight: 800; }\nh3 { font-size: clamp(1.5rem,  1.5vw + 1.15rem,  2.5rem);  line-height: 1.2;  letter-spacing: -0.02em;  font-weight: 700; }\nh4 { font-size: clamp(1.25rem, 0.75vw + 1.07rem, 1.75rem); line-height: 1.3;  letter-spacing: -0.015em; font-weight: 700; }\nh5 { font-size: clamp(1.125rem, 0.375vw + 1.04rem, 1.375rem); line-height: 1.35; font-weight: 600; }\nh6 { font-size: clamp(1rem, 0.19vw + 0.96rem, 1.125rem);    line-height: 1.4;  font-weight: 600; }\n/* Font: Inter Tight · Colour: --color-headline · Applied automatically */"}</code></pre>
            </div>

            <div class="type-preview px-8 py-2 overflow-x-hidden">
              {headings.map((h, i) => (
                <div class={`py-6 flex flex-col gap-2${i < headings.length - 1 ? ' border-b border-[--color-border]' : ''}`}>
                  <div class="flex items-center gap-4 flex-wrap">
                    <span class="text-xs font-bold font-mono text-[--color-caption] uppercase tracking-widest w-6 shrink-0">{h.label}</span>
                    <code class="code-hex">{h.size}</code>
                    <span class="text-xs text-[--color-caption]">weight {h.weight}</span>
                    <span class="text-xs text-[--color-caption]">lh {h.lh}</span>
                    <span class="text-xs text-[--color-caption] hidden sm:block ml-auto">{h.use}</span>
                  </div>
                  {h.tag === "h1" && <h1 class="m-0 overflow-hidden text-ellipsis">{h.label} — Expert Clinic Care</h1>}
                  {h.tag === "h2" && <h2 class="m-0">{h.label} — Expert Clinic Care</h2>}
                  {h.tag === "h3" && <h3 class="m-0">{h.label} — Expert Clinic Care</h3>}
                  {h.tag === "h4" && <h4 class="m-0">{h.label} — Expert Clinic Care</h4>}
                  {h.tag === "h5" && <h5 class="m-0">{h.label} — Expert Clinic Care</h5>}
                  {h.tag === "h6" && <h6 class="m-0">{h.label} — Expert Clinic Care</h6>}
                </div>
              ))}
            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Headings</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Update the H1 heading size. Change --text-h1 in the @theme block of src/styles/global.css. Use the clamp() format: clamp(min, preferred, max). Also update --text-h1--line-height and --text-h1--letter-spacing if needed. Do not change any other heading variables unless asked.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               04 — BODY TEXT SYSTEM
          ─────────────────────────────────────────────────────── -->
          <article id="body-text" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">05</span>
                <h5 class="m-0">Body Text System</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Body</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-0">
              {textStyles.map((t, i) => (
                <div class={`flex flex-col gap-3 py-6${i < textStyles.length - 1 ? ' border-b border-[--color-border]' : ''}`}>
                  <div class="flex items-center gap-3 flex-wrap">
                    <code class="code-label">{t.token}</code>
                    <span class="text-xs text-[--color-caption]">{t.px} · lh {t.lh}</span>
                    <span class="text-xs font-medium text-[--color-caption]">{t.label} — {t.desc}</span>
                  </div>
                  <p class={`m-0 text-[--color-body] ${t.cls}`}>{t.sample}</p>
                </div>
              ))}
            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Body Text</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">The body text uses --text-base (16px) and --color-body (#151515) set globally in src/styles/global.css. Paragraphs inherit these automatically via @layer base. To change colour update --color-body. To change size update --text-base. Do not go below 16px. Line-height is --text-base--line-height (1.75).</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               05 — BUTTON SYSTEM
          ─────────────────────────────────────────────────────── -->
          <article id="buttons" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">06</span>
                <h5 class="m-0">Button System</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Buttons</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- Light zone -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">On Light Backgrounds</p>
                <div class="bg-[--color-surface] border border-[--color-border] rounded-[--radius-card] p-6 flex flex-col gap-5">
                  {buttons.filter(b => !b.dark).map(b => (
                    <div class="flex items-start gap-5 flex-wrap">
                      <div class="shrink-0 min-w-[10rem]">
                        <a href="#" class={`btn ${b.cls}`}>.{b.cls}</a>
                      </div>
                      <div class="flex flex-col gap-1 flex-1">
                        <code class="code-label">.btn .{b.cls}</code>
                        <p class="m-0 text-sm text-[--color-body]">{b.desc}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Dark zone -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">On Dark Backgrounds</p>
                <div class="ds-dark rounded-xl p-6 flex flex-col gap-5">
                  {buttons.filter(b => b.dark).map(b => (
                    <div class="flex items-start gap-5 flex-wrap">
                      <div class="shrink-0 min-w-[10rem]">
                        <a href="#" class={`btn ${b.cls}`}>.{b.cls}</a>
                      </div>
                      <div class="flex flex-col gap-1 flex-1">
                        <code class="code-label">.btn .{b.cls}</code>
                        <p class="m-0 text-sm text-white/60">{b.desc}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Usage -->
              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">Usage</p>
                <pre class="code-block"><code>&lt;a href="#" class="btn btn-default"&gt;Book Appointment&lt;/a&gt;       /* light bg */
&lt;a href="#" class="btn btn-secondary"&gt;Learn More&lt;/a&gt;          /* light bg */
&lt;a href="#" class="btn btn-white"&gt;View Services&lt;/a&gt;           /* dark bg  */
&lt;a href="#" class="btn btn-transparent"&gt;Read More&lt;/a&gt;         /* dark bg  */
&lt;button type="submit" class="btn btn-default w-full"&gt;Send&lt;/button&gt;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Buttons</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Add a [BUTTON TYPE] button to [SECTION NAME]. Always use both .btn base and a variant class (btn-default, btn-secondary, btn-white, or btn-transparent). Never create new button styles. Buttons are in src/styles/global.css @layer components. Height is 48px and radius is 10px — do not change these.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               06 — FORM ELEMENTS
          ─────────────────────────────────────────────────────── -->
          <article id="forms" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">07</span>
                <h5 class="m-0">Form Elements</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Forms</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <p class="m-0 text-[--color-body]">All contact forms use the reusable <code class="code-label">&lt;ContactForm /&gt;</code> component (Web3Forms-powered). All inputs use the <code class="code-label">.input</code> class. Radius matches buttons (10px). Focus ring uses <code class="code-hex">--color-primary</code> with a 3px offset ring.</p>

              <ContactForm id="docs" />

              <div class="border-t border-[--color-border] pt-5">
                <pre class="code-block"><code>&lt;ContactForm
  subjectPrefix="ClinicName"
  brandName="ClinicName Website"
  id="contact-page"
/&gt;

{/* For lower-level usage, inputs use: */}
&lt;label class="text-sm font-semibold text-[--color-headline]"&gt;Label&lt;/label&gt;
&lt;input  type="text"  class="input" placeholder="..." /&gt;
&lt;select              class="input"&gt;...&lt;/select&gt;
&lt;textarea            class="input" rows="4"&gt;&lt;/textarea&gt;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Forms</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Add a contact form using the &lt;ContactForm /&gt; component from src/components/ContactForm.astro. Pass subjectPrefix, brandName, and a unique id prop. It is Web3Forms-powered (PUBLIC_WEB3FORMS_KEY env var required). Never build a new form from scratch and never use Netlify Forms, Formspree, or custom handlers. For non-contact form fields, use the .input class on all inputs, selects, and textareas. Labels use class="text-sm font-semibold text-[--color-headline]". Wrap each label + input in a flex flex-col gap-2 div.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               07 — CONTAINER SYSTEM
          ─────────────────────────────────────────────────────── -->
          <article id="containers" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">08</span>
                <h5 class="m-0">Container System</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Layout</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="border-2 border-dashed border-[--color-caption]/40 rounded-[--radius-card] p-4">
                <p class="text-xs font-mono text-[--color-caption] m-0 mb-3">&lt;section&gt; — Full page width · background colour lives here</p>
                <div class="border-2 border-dashed border-[--color-primary]/50 rounded-[--radius-card] p-4">
                  <p class="text-xs font-mono text-[--color-primary] m-0 mb-3">.container-main — max-width 1340px · padding: 90px/30px desktop · 70px/30px mobile</p>
                  <div class="bg-[--color-muted] border-2 border-dashed border-[--color-headline]/40 rounded-[--radius-card] p-4">
                    <p class="text-xs font-mono text-[--color-headline] m-0">.container-inner — no padding · content sits flush</p>
                  </div>
                </div>
              </div>

              <div class="flex flex-col gap-3">
                {[
                  { cls: ".container-main",  desc: "Primary section wrapper. Max-width 1340px, centred. Padding: 90px 30px desktop, 70px 30px mobile." },
                  { cls: ".container-inner", desc: "Inner content wrapper. No padding. Always nest inside .container-main." },
                  { cls: ".section",         desc: "Alias for .container-main. Use on semantic <section> elements." },
                  { cls: ".container",       desc: "Max-width 1340px, centred, no padding. Use in nav, footer, and already-padded wrappers." },
                ].map(item => (
                  <div class="flex items-start gap-3 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="icon-dot-offset"></div>
                    <div class="flex flex-col gap-1">
                      <code class="code-label">{item.cls}</code>
                      <p class="m-0 text-sm text-[--color-body]">{item.desc}</p>
                    </div>
                  </div>
                ))}
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Containers</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Build a new [SECTION NAME] section using the global container system. Wrap in &lt;section class="bg-[COLOUR]"&gt;. Inside use &lt;div class="container-main"&gt; for padding and max-width. Inside that use &lt;div class="container-inner"&gt; for content. Max-width 1340px. Do not add custom padding — use the container system only.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               08 — FLEX LAYOUT SYSTEM
          ─────────────────────────────────────────────────────── -->
          <article id="flex" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">09</span>
                <h5 class="m-0">Flex Layout System</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Flex</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <p class="m-0 text-[--color-body]">Flexbox is the default layout system. Use <code class="code-label">.flex-layout</code> for all rows. Default gap is 15px (<code class="code-hex">--spacing-gap</code>). Override only when needed.</p>

              <div class="flex flex-col gap-4">
                {[
                  { cls: ".flex-layout",                     desc: "Default · 15px gap · wraps on small screens",     items: ["Item A", "Item B", "Item C", "Item D"], mod: "flex-layout" },
                  { cls: ".flex-layout.flex-layout--row",    desc: "Explicit horizontal row · same as default but declared clearly", items: ["Left", "Centre", "Right"], mod: "flex-layout flex-layout--row" },
                  { cls: ".flex-layout.flex-layout--center", desc: "Centre-aligned · trust badges, CTA rows",         items: ["Trust A", "Trust B", "Trust C"],        mod: "flex-layout flex-layout--center" },
                  { cls: ".flex-layout.flex-layout--between",desc: "Space-between · nav bars, header rows",           items: ["Logo", "Nav Links", "CTA Button"],      mod: "flex-layout flex-layout--between" },
                  { cls: ".flex-layout.flex-layout--col",    desc: "Vertical stack · form fields, info blocks",       items: ["Stack 1", "Stack 2", "Stack 3"],         mod: "flex-layout flex-layout--col" },
                  { cls: ".flex-layout.flex-layout--start",  desc: "Top-align items · text+image rows, card columns", items: ["Top A", "Top B", "Top C"],              mod: "flex-layout flex-layout--start" },
                ].map(demo => (
                  <div class="flex flex-col gap-2">
                    <div class="flex items-center gap-3 flex-wrap">
                      <code class="code-label">{demo.cls}</code>
                      <span class="text-xs text-[--color-caption]">{demo.desc}</span>
                    </div>
                    <div class={`${demo.mod} p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]`}>
                      {demo.items.map(item => (
                        <div class="bg-[--color-accent] text-[--color-primary] text-xs font-semibold px-3 py-2 rounded border border-[--color-primary]/20">{item}</div>
                      ))}
                    </div>
                  </div>
                ))}
              </div>

              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-4">Gap Scale</p>
                <div class="flex flex-col gap-2">
                  {[
                    { token: "--spacing-gap",    px: "15px", use: "Default (set automatically)" },
                    { token: "--spacing-gap-md", px: "24px", use: "Cards, grids — override: gap-[--spacing-gap-md]" },
                    { token: "--spacing-gap-lg", px: "32px", use: "Section columns — override: gap-[--spacing-gap-lg]" },
                  ].map(g => (
                    <div class="flex items-center gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                      <code class="code-label">{g.token}</code>
                      <code class="code-hex">{g.px}</code>
                      <span class="text-sm text-[--color-body]">{g.use}</span>
                    </div>
                  ))}
                </div>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Flex Layout</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Build a [DESCRIPTION] layout inside [SECTION]. Use &lt;div class="flex-layout"&gt; for the container. Default gap is 15px. Override with gap-[--spacing-gap-md] for 24px or gap-[--spacing-gap-lg] for 32px. Add .flex-layout--center, --between, --col, or --start modifiers as needed. Do not create new flex classes.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               09 — CARD COMPONENT
          ─────────────────────────────────────────────────────── -->
          <article id="cards" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">10</span>
                <h5 class="m-0">Card Component</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Cards</span>
            </header>

            <div class="bg-[--color-accent] px-8 py-8 border-b border-[--color-border]">
              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
                {features.map(f => (
                  <div class="card">
                    <div class="icon-dot mb-4"></div>
                    <h5 class="mt-0 mb-2">{f.title}</h5>
                    <p class="m-0 text-sm text-[--color-body]">{f.desc}</p>
                  </div>
                ))}
              </div>
            </div>

            <div class="px-8 py-6 flex flex-col gap-5">

              <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                {[
                  { prop: "background",    val: "var(--color-white)" },
                  { prop: "border",        val: "1px solid var(--color-border)" },
                  { prop: "border-radius", val: "var(--radius-card) · 12px" },
                  { prop: "padding",       val: "var(--spacing-card-p) · 28px" },
                  { prop: "box-shadow",    val: "var(--shadow-card)" },
                  { prop: "hover",         val: "translateY(-2px) + shadow-raised + border-primary" },
                ].map(p => (
                  <div class="flex items-center gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <code class="code-label shrink-0">{p.prop}</code>
                    <span class="text-xs text-[--color-caption]">{p.val}</span>
                  </div>
                ))}
              </div>

              <div class="border-t border-[--color-border] pt-5">
                <pre class="code-block"><code>&lt;div class="card"&gt;
  &lt;h4&gt;Card Title&lt;/h4&gt;
  &lt;p&gt;Card description.&lt;/p&gt;
&lt;/div&gt;
/* Always place .card on a muted/accent background — never on white */
/* Hover animation is built in — no extra classes needed            */</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Cards</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Add a [N]-card grid to [SECTION]. Use &lt;div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"&gt; as the wrapper. Each card uses &lt;div class="card"&gt;. Place the grid inside a muted or accent background section. Do not add custom card styles — .card handles all styling and hover.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               10 — COMBINED EXAMPLE
          ─────────────────────────────────────────────────────── -->
          <article id="combined" class="border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] bg-white flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">11</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Combined Example — Real Clinic Section</h5>
                  <p class="text-sm text-[--color-caption] m-0">Every part of the design system working together.</p>
                </div>
              </div>
            </header>

            <section class="bg-[--color-accent]">
              <div class="container-main">
                <div class="container-inner">
                  <div class="section-header">
                    <span class="eyebrow">Why Choose Us</span>
                    <h2>Expert Care You Can Trust</h2>
                    <p class="section-lead">Our team of HCPC-registered physiotherapists bring decades of combined experience helping patients recover faster, move better, and live without pain.</p>
                  </div>
                  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5 mb-12">
                    {features.map(f => (
                      <div class="card">
                        <div class="icon-dot mb-3"></div>
                        <h4 class="mt-0 mb-2">{f.title}</h4>
                        <p class="m-0 text-sm">{f.desc}</p>
                      </div>
                    ))}
                  </div>
                  <div class="flex-layout flex-layout--center gap-4">
                    <a href="#" class="btn btn-default">Book Your Appointment</a>
                    <a href="#" class="btn btn-secondary">View All Services</a>
                  </div>
                </div>
              </div>
            </section>

            <div class="bg-white border-t border-[--color-border] px-8 py-6">
              <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">Source Structure</p>
              <pre class="code-block"><code>&lt;section class="bg-[--color-accent]"&gt;
  &lt;div class="container-main"&gt;
    &lt;div class="container-inner"&gt;
      &lt;div class="section-header"&gt;
        &lt;span class="eyebrow"&gt;Why Choose Us&lt;/span&gt;
        &lt;h2&gt;Expert Care You Can Trust&lt;/h2&gt;
        &lt;p class="section-lead"&gt;...&lt;/p&gt;
      &lt;/div&gt;
      &lt;div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5"&gt;
        &lt;div class="card"&gt;...&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class="flex-layout flex-layout--center gap-4"&gt;
        &lt;a class="btn btn-default"&gt;Book&lt;/a&gt;
        &lt;a class="btn btn-secondary"&gt;Learn More&lt;/a&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/section&gt;</code></pre>
            </div>

            <div class="bg-white px-8 pb-8">
              <div class="rounded-[--radius-card] overflow-hidden">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — New Section</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">Build a new [SECTION NAME] section for the clinic website. Use the global design system only — no new CSS classes. Structure: &lt;section class="bg-[COLOUR]"&gt; wrapping .container-main wrapping .container-inner. Use .section-header with .eyebrow for the intro. Use .card for features. Use .btn.btn-default for primary CTA and .btn.btn-secondary for secondary. Use existing colour variables only.</p>
                </div>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               11 — UTILITY CLASSES
          ─────────────────────────────────────────────────────── -->
          <article id="utilities" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">12</span>
                <h5 class="m-0">Utility Classes</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Utils</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-5">

              {[
                { cls: ".eyebrow",       preview: "eyebrow",     desc: "Small uppercase label above section headings. Use inside .section-header or standalone above an h2." },
                { cls: ".section-lead",  preview: "section-lead", desc: "Large intro paragraph below a heading. 18px, relaxed line-height. Use inside .section-header." },
                { cls: ".section-header",preview: null,           desc: "Wrapper for eyebrow + heading + section-lead. Centres content and adds bottom margin. Use at the top of every section." },
                { cls: ".caption-light", preview: "caption-light",desc: "Small secondary label. Uses --color-caption. For prompt box labels, metadata, and non-critical supporting text on light backgrounds." },
                { cls: ".icon-dot",      preview: null,           desc: "Small primary-coloured decorative bullet. Used inside cards above the title. 10px circle in --color-primary." },
                { cls: ".code-label",    preview: "code-label",   desc: "Inline code pill. Off-white background with border. For CSS variable names and class names in documentation." },
                { cls: ".code-hex",      preview: "#DB003E",      desc: "Inline hex/value badge. Muted background. For values, sizes, and short references." },
                { cls: ".copy-btn",      preview: "Copy",         desc: "Copy to clipboard button. Used in all Claude AI Prompt boxes. Handles active state via JS." },
              ].map(u => (
                <div class="flex items-start gap-5 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                  <div class="shrink-0 min-w-[9rem] flex items-center">
                    {u.preview === "eyebrow"       && <span class="eyebrow">Eyebrow</span>}
                    {u.preview === "section-lead"  && <p class="section-lead text-sm m-0">Lead text sample.</p>}
                    {u.preview === "caption-light" && <span class="caption-light">Caption text</span>}
                    {u.preview === "code-label"    && <code class="code-label">.class-name</code>}
                    {u.preview === "#DB003E"        && <code class="code-hex">#DB003E</code>}
                    {u.preview === "Copy"           && <button type="button" class="copy-btn">Copy</button>}
                    {u.preview === null             && <span class="text-xs text-[--color-caption] italic">Layout only</span>}
                  </div>
                  <div class="flex flex-col gap-1 flex-1">
                    <code class="code-label">{u.cls}</code>
                    <p class="m-0 text-sm text-[--color-body]">{u.desc}</p>
                  </div>
                </div>
              ))}

              <div class="border-t border-[--color-border] pt-5">
                <pre class="code-block"><code>&lt;div class="section-header"&gt;
  &lt;span class="eyebrow"&gt;Why Choose Us&lt;/span&gt;
  &lt;h2&gt;Heading Here&lt;/h2&gt;
  &lt;p class="section-lead"&gt;Intro paragraph.&lt;/p&gt;
&lt;/div&gt;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Utility Classes</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Use global utility classes for section structure. Every section heading block must use .section-header containing .eyebrow, h2, and .section-lead. Do not create new heading wrapper classes. Use .icon-dot inside cards above the h4/h5. Use .caption-light for prompt labels and metadata. Do not invent new utility classes — extend or reuse existing ones only.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               12 — DESIGN TOKENS
          ─────────────────────────────────────────────────────── -->
          <article id="tokens" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">13</span>
                <h5 class="m-0">Design Tokens — Radius, Shadows, Spacing</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Tokens</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-8">

              <!-- Radius -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Border Radius</p>
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                  {[
                    { token: "--radius-card",  val: "12px",   label: ".card, article, prompt boxes" },
                    { token: "--radius-btn",   val: "10px",   label: ".btn, .input, sidebar links" },
                    { token: "--radius-badge", val: "999px",  label: ".eyebrow, badges, pills, tags" },
                  ].map(r => (
                    <div class="radius-demo flex flex-col gap-3 p-4 bg-[--color-muted] border border-[--color-border]" data-radius={r.val}>
                      <code class="code-label">{r.token}</code>
                      <code class="code-hex">{r.val}</code>
                      <p class="text-xs text-[--color-caption] m-0">{r.label}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Shadows -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Shadows</p>
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                  {[
                    { token: "--shadow-card",   label: "Default card shadow",    cls: "shadow-[--shadow-card]"   },
                    { token: "--shadow-raised",  label: "Card hover raised state", cls: "shadow-[--shadow-raised]"  },
                    { token: "--shadow-nav",     label: "Sticky nav underline",   cls: "shadow-[--shadow-nav]"    },
                  ].map(s => (
                    <div class={`flex flex-col gap-3 p-4 bg-white border border-[--color-border] rounded-[--radius-card] ${s.cls}`}>
                      <code class="code-label">{s.token}</code>
                      <p class="text-xs text-[--color-caption] m-0">{s.label}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Spacing -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Spacing Tokens</p>
                <div class="flex flex-col gap-2">
                  {[
                    { token: "--spacing-section-x", val: "30px",  use: "Horizontal padding on footer and full-width elements" },
                    { token: "--spacing-card-p",    val: "28px",  use: "Internal padding on .card components" },
                    { token: "--spacing-gap",        val: "15px",  use: "Default flex gap in .flex-layout" },
                    { token: "--spacing-gap-md",     val: "24px",  use: "Medium gap — cards and grids" },
                    { token: "--spacing-gap-lg",     val: "32px",  use: "Large gap — section columns" },
                  ].map(s => (
                    <div class="flex items-center gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                      <code class="code-label shrink-0">{s.token}</code>
                      <code class="code-hex shrink-0">{s.val}</code>
                      <span class="text-sm text-[--color-body]">{s.use}</span>
                    </div>
                  ))}
                </div>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Tokens</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Use global design tokens for all radius, shadow, and spacing. Border radius: --radius-card (12px) for cards, --radius-btn (10px) for buttons and inputs, --radius-badge (999px) for pills and badges. Shadows: --shadow-card on cards, --shadow-raised on hover states. Spacing: --spacing-card-p (28px) for card padding, --spacing-gap (15px) default flex gap. Do not hardcode pixel values when a token exists.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               13 — GLOBAL.CSS STRUCTURE
          ─────────────────────────────────────────────────────── -->
          <article id="globalcss" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">14</span>
                <h5 class="m-0">global.css Structure</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">CSS</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <p class="m-0 text-[--color-body]">All design tokens and global styles live in <code class="code-label">src/styles/global.css</code>. The file is split into four blocks. Only edit the correct block for your task.</p>

              <div class="flex flex-col gap-3">
                {[
                  {
                    block: "@theme",
                    file: "src/styles/global.css",
                    desc: "All CSS custom properties (design tokens). Colours, fonts, radius, shadows, spacing. Change brand colour, fonts, or spacing here. This is the only place tokens should be defined.",
                    when: "When changing brand colour, font, radius, shadow, or spacing values.",
                  },
                  {
                    block: "@layer base",
                    file: "src/styles/global.css",
                    desc: "Global element resets and default styles. Headings, paragraphs, anchors, and form elements. Applied automatically — no class needed. Heading sizes, line-heights, and colours are set here.",
                    when: "When changing default heading sizes, body text size, or element-level defaults.",
                  },
                  {
                    block: "@layer components",
                    file: "src/styles/global.css",
                    desc: "Reusable class-based components. .btn, .card, .input, .eyebrow, .section-header, .flex-layout, .container-main, .container-inner, etc. All reusable UI patterns live here.",
                    when: "When adding or modifying a reusable class. Never add one-off classes here.",
                  },
                  {
                    block: "@layer utilities",
                    file: "src/styles/global.css",
                    desc: "Custom utility classes that extend Tailwind. Use sparingly. Only add here when a Tailwind utility does not exist for the needed pattern.",
                    when: "Only when Tailwind cannot achieve the pattern with existing utilities.",
                  },
                ].map(b => (
                  <div class="p-5 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex flex-col gap-2">
                    <div class="flex items-center gap-3 flex-wrap">
                      <code class="code-label">{b.block}</code>
                      <code class="code-hex">{b.file}</code>
                    </div>
                    <p class="m-0 text-sm text-[--color-body]">{b.desc}</p>
                    <p class="m-0 text-xs text-[--color-caption]"><strong>Edit when:</strong> {b.when}</p>
                  </div>
                ))}
              </div>

              <div class="border-t border-[--color-border] pt-5">
                <pre class="code-block"><code>/* src/styles/global.css — structure overview */

@theme &lbrace;
  /* ── Tokens: colours, fonts, radius, shadows, spacing ── */
  --color-primary: #DB003E;
  --font-heading: 'Inter Tight', sans-serif;
  --radius-card: 12px;
&rbrace;

@layer base &lbrace;
  /* ── Global element defaults (h1–h6, p, a, input) ── */
  h1 &lbrace; font-size: clamp(...); &rbrace;
&rbrace;

@layer components &lbrace;
  /* ── Reusable classes (.btn, .card, .input, etc.) ── */
  .btn &lbrace; ... &rbrace;
  .card &lbrace; ... &rbrace;
&rbrace;

@layer utilities &lbrace;
  /* ── Custom utilities (use sparingly) ── */
&rbrace;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — global.css</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">When adding a new design token, add it to the @theme block in src/styles/global.css. When adding a reusable class, add it to @layer components. When changing a heading default, edit @layer base. Never add one-off styles to global.css. Never add styles to individual page files unless scoped to that page only.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               14 — IMAGE FORMAT RULES
          ─────────────────────────────────────────────────────── -->
          <article id="images" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">15</span>
                <h5 class="m-0">Image Format Rules</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Images</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="flex flex-col gap-3">
                {[
                  { badge: "1st",   fmt: ".avif",  label: "Primary format",  desc: "Always use as the primary <source>. Best compression and quality. Assumed to exist for every image." },
                  { badge: "2nd",   fmt: ".webp",  label: "Fallback format", desc: "Always include as the second <source>. Wider browser support." },
                  { badge: "Only if needed", fmt: ".jpg / .png", label: "Last resort", desc: "Only use if no modern format is available or there is a clear technical reason. Never as primary." },
                ].map(f => (
                  <div class="flex items-start gap-4 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <span class="text-xs font-bold text-[--color-primary] bg-[--color-accent] border border-[--color-primary]/20 px-2 py-1 rounded-[--radius-badge] shrink-0 whitespace-nowrap">{f.badge}</span>
                    <div class="flex flex-col gap-1">
                      <div class="flex items-center gap-2">
                        <code class="code-label">{f.fmt}</code>
                        <span class="text-xs text-[--color-caption]">{f.label}</span>
                      </div>
                      <p class="m-0 text-sm text-[--color-body]">{f.desc}</p>
                    </div>
                  </div>
                ))}
              </div>

              <div class="p-4 bg-[--color-accent] border border-[--color-primary]/20 rounded-[--radius-card] flex gap-3 items-start">
                <span class="text-[--color-primary] font-bold text-sm shrink-0">⚠</span>
                <p class="m-0 text-sm text-[--color-body]"><strong>If a developer uploads .jpg or .png:</strong> Show this message — "Fel recommends uploading .avif images. Use this converter: <code class="code-label">https://hmdg-elementor.flywheelsites.com/</code>"</p>
              </div>

              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Loading Rules</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { attr: 'loading="eager"',   use: "Hero and above-the-fold images only" },
                    { attr: 'loading="lazy"',    use: "All below-fold images — default" },
                    { attr: 'decoding="async"',  use: "Always include on all images" },
                    { attr: 'width="" height=""',use: "Always set to prevent CLS (layout shift)" },
                  ].map(a => (
                    <div class="flex flex-col gap-1 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <code class="code-label">{a.attr}</code>
                      <p class="text-xs text-[--color-caption] m-0">{a.use}</p>
                    </div>
                  ))}
                </div>
              </div>

              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">Required Pattern</p>
                <pre class="code-block"><code>&lt;picture&gt;
  &lt;source srcset="/images/hero.avif" type="image/avif" /&gt;
  &lt;source srcset="/images/hero.webp" type="image/webp" /&gt;
  &lt;img
    src="/images/hero.webp"
    alt="Descriptive alt text"
    loading="lazy"
    decoding="async"
    width="1600"
    height="900"
  /&gt;
&lt;/picture&gt;

/* Use loading="eager" for hero / above-fold images only */</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Images</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Add an image to [SECTION]. Use the required picture pattern: &lt;picture&gt; with .avif as the first source, .webp as the second source, and a &lt;img&gt; fallback with loading="lazy" decoding="async" and explicit width and height. Use loading="eager" only for the hero image. Never use &lt;img&gt; alone without the &lt;picture&gt; wrapper when .avif and .webp are available.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               15 — SECURITY REFERENCE
          ─────────────────────────────────────────────────────── -->
          <article id="security" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">16</span>
                <h5 class="m-0">Security Reference</h5>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide">Security</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-5">

              {[
                { rule: "No secrets in frontend code",      detail: "API keys, tokens, and credentials must never appear in .astro, .ts, or .js files. Use environment variables only." },
                { rule: "Use .env for all secrets",         detail: "Copy .env.example to .env and fill in values. Never commit .env to git — it is already in .gitignore." },
                { rule: "External links — rel attribute",   detail: 'All <a> tags with target="_blank" must include rel="noopener noreferrer" to prevent tab-napping attacks.' },
                { rule: "No unsafe HTML injection",         detail: "Never use set:html or innerHTML with unsanitised user input. Only use with trusted, controlled content." },
                { rule: "No inline event handlers",         detail: "Never use onclick='', onload='', or similar inline handlers. Use addEventListener in script blocks only." },
                { rule: "CSP headers are pre-configured",   detail: "Content Security Policy is set in BaseLayout.astro (meta tag), public/_headers (Netlify), and vercel.json (Vercel). Update these when adding new third-party services." },
                { rule: "Forms need spam prevention",       detail: "All public forms should include a honeypot field or reCAPTCHA. Never rely on client-side validation alone." },
                { rule: "OWASP Top 10 applies at all times",detail: "Follow secure coding defaults. No SQL injection risk, XSS risk, or data exposure in any frontend output." },
              ].map(r => (
                <div class="flex items-start gap-4 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <div class="icon-dot-offset mt-1"></div>
                  <div class="flex flex-col gap-1">
                    <span class="text-sm font-semibold text-[--color-headline]">{r.rule}</span>
                    <p class="m-0 text-sm text-[--color-body]">{r.detail}</p>
                  </div>
                </div>
              ))}

              <div class="border-t border-[--color-border] pt-5">
                <pre class="code-block"><code>/* Security files in this template */
public/_headers     → Netlify HTTP security headers (CSP, HSTS, X-Frame-Options)
vercel.json         → Vercel HTTP security headers (same set)
.env.example        → Copy to .env — never commit .env
BaseLayout.astro    → CSP meta tag in &lt;head&gt; for all pages

/* Required pattern for all external links */
&lt;a href="https://..." target="_blank" rel="noopener noreferrer"&gt;Link&lt;/a&gt;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Security</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Review [PAGE/COMPONENT] for security issues. Check: no secrets in markup, all external links have rel="noopener noreferrer", no unsafe set:html or innerHTML, no inline event handlers, forms include spam prevention, environment variables are used for all sensitive values. Flag any issues with severity level and suggest fixes.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               16 — AGENT SYSTEM
          ─────────────────────────────────────────────────────── -->
          <article id="agents" class="border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] bg-white flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">17</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Agent System</h5>
                  <p class="text-sm text-[--color-caption] m-0">12 specialist AI agents. Each one has a single clear job. Use the review sequence below for every important page build.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Agents</span>
            </header>

            <div class="px-8 py-6 bg-white flex flex-col gap-6">

              <!-- Plain-English summary -->
              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Each agent is a specialist that handles ONE thing well — like a relay race where every runner has their own job. Big tasks get passed through several agents in order. Smaller tasks only need one. The list below tells you who handles what.
                </p>
              </div>

              <!-- Review Sequence -->
              <div class="flex flex-col gap-2">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Standard Review Sequence (Tier 4–5 page builds)</p>
                <div class="flex flex-wrap gap-2 items-center">
                  {[
                    "information-architecture-reviewer",
                    "ux-architect",
                    "ui-designer",
                    "frontend-builder",
                    "a11y-reviewer",
                    "performance-reviewer",
                    "performance-optimisation",
                    "seo-reviewer",
                    "marketing-reviewer",
                    "security-reviewer",
                    "conversion-reviewer",
                  ].map((agent, i, arr) => (
                    <div class="flex items-center gap-2">
                      <code class="code-label text-[0.65rem]">{agent}</code>
                      {i < arr.length - 1 && <span class="text-[--color-caption] text-xs">→</span>}
                    </div>
                  ))}
                </div>
              </div>

              <!-- Agent cards -->
              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                {[
                  { num: "01", name: "information-architecture-reviewer", call: "When planning a new page, sitemap, or URL structure",
                    role: "Decides URL hierarchy, parent/child pages, taxonomy. Confirms the page belongs where you're putting it. Runs BEFORE any design work." },
                  { num: "02", name: "ux-architect", call: "When mapping user flow before visual design",
                    role: "Maps the user journey (Awareness → Interest → Trust → Intent → Action). Defines what each section needs to achieve and how interactions behave. Plans only — no code." },
                  { num: "03", name: "ui-designer", call: "When planning visual layout / colour / spacing / typography",
                    role: "Section anatomy, layout structure, token-based colour planning, mobile-first sizing, interactions. Hands a written spec to the builder. Does NOT write code." },
                  { num: "04", name: "frontend-builder", call: "When writing or editing Astro / Tailwind code",
                    role: "Builds the approved design in clean Astro + Tailwind. Uses existing class system, BaseLayout, semantic HTML, reusable components. No inline styles, no AVIF/JPG (WebP only)." },
                  { num: "05", name: "a11y-reviewer", call: "After frontend-builder has shipped a feature",
                    role: "Checks WCAG 2.1 AA — 4.5:1 contrast, visible focus rings, heading order, keyboard navigation, form labels, 44×44 touch targets, prefers-reduced-motion fallbacks." },
                  { num: "06", name: "performance-reviewer", call: "Quick perf check after a feature ships",
                    role: "Image formats, DOM weight, lazy loading, JS bundle size, CLS risk. Flags issues but doesn't run the deep audit." },
                  { num: "07", name: "performance-optimisation", call: "Deep speed audit / before launch",
                    role: "Full Core Web Vitals audit (LCP, CLS, INP), third-party script weight review, hydration strategy. Targets 90+ on PageSpeed mobile. Runs deeper than performance-reviewer." },
                  { num: "08", name: "seo-reviewer", call: "After build, before launch",
                    role: "H1 hierarchy, meta description, canonical, noindex rules, internal links, structured data (JSON-LD), local SEO signals (NAP, opening hours)." },
                  { num: "09", name: "marketing-reviewer", call: "When messaging / copy / trust signals need review",
                    role: "Value proposition clarity, trust signals (Google rating, accreditations, years in business), service positioning. Applies copy fixes directly." },
                  { num: "10", name: "security-reviewer", call: "When a form, embed, third-party script, or env var is touched",
                    role: "OWASP Top 10 frontend issues. Forms, scripts, unsafe HTML, exposed secrets, external links missing rel=noopener, CSP coverage." },
                  { num: "11", name: "conversion-reviewer", call: "Final gate before declaring a page done",
                    role: "CTA clarity, CTA placement, booking flow, friction points, overall journey. Last in the chain — assesses the page in its final state." },
                  { num: "12", name: "visual-qa-reviewer", call: "Ad-hoc when built output looks off",
                    role: "Supplementary — checks the built result matches the design plan. Catches implementation gaps between ui-designer's spec and what shipped." },
                ].map(a => (
                  <div class="flex flex-col gap-2 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="flex items-center gap-2">
                      <span class="text-xs font-mono font-bold text-[--color-primary]">{a.num}</span>
                      <code class="code-label text-[0.65rem] leading-tight">{a.name}</code>
                    </div>
                    <p class="m-0 text-[10px] font-semibold text-[--color-primary] uppercase tracking-wide">CALL THIS AGENT: {a.call}</p>
                    <p class="m-0 text-xs text-[--color-body] leading-relaxed">{a.role}</p>
                  </div>
                ))}
              </div>

              <!-- Escalation -->
              <div class="border-t border-[--color-border] pt-5 flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Escalation — when a reviewer finds an issue</p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  {[
                    { trigger: "Visual / styling problem", path: "reviewer flags → ui-designer redesigns → frontend-builder rebuilds" },
                    { trigger: "Code-only fix (missing aria-label, rel=noopener, lazy-loading)", path: "reviewer flags → frontend-builder fixes" },
                    { trigger: "Copy / messaging weakness", path: "marketing-reviewer applies fix directly" },
                    { trigger: "Critical security or accessibility issue", path: "fix + verify with a SECOND pass by the same reviewer" },
                  ].map(e => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <p class="m-0 text-xs font-semibold text-[--color-headline]">{e.trigger}</p>
                      <p class="m-0 mt-1 text-xs text-[--color-body] leading-relaxed">{e.path}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Parallel waves -->
              <div class="border-t border-[--color-border] pt-5 flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Parallel vs Sequential</p>
                <p class="m-0 text-sm text-[--color-body]">Planning agents (IA → UX → UI → builder) MUST run in order — each one feeds the next. Reviewer agents AFTER the build can run in waves:</p>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">Wave 1 — parallel</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">a11y-reviewer · performance-reviewer · seo-reviewer</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">Wave 2 — parallel</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">performance-optimisation · marketing-reviewer · security-reviewer</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">Wave 3 — final</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">conversion-reviewer (sees the page in its final state)</p>
                  </div>
                </div>
              </div>

            </div>

            <div class="bg-white px-8 pb-8">
              <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — Agent Workflow</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">Review and build this task using the following agent sequence exactly: information-architecture-reviewer → ux-architect → ui-designer → frontend-builder → a11y-reviewer → performance-reviewer → performance-optimisation → seo-reviewer → marketing-reviewer → security-reviewer → conversion-reviewer. Task: [describe the page, section, sitemap, or feature here]. Do not skip any agent. Do not rush into code before planning. If something is weak, improve it automatically.</p>
                </div>
              </div>
            </div>

          </article>



          <!-- ───────────────────────────────────────────────────
               17 — MODES & AGENT TIERS
          ─────────────────────────────────────────────────────── -->
          <article id="modes" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">18</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Modes & Agent Tiers</h5>
                  <p class="text-sm text-[--color-caption] m-0">How Claude decides what to do, how big a task is, and which agents to call.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Workflow</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Modes set the speed (fast vs careful). Tiers set the size (small vs large). The combination tells Claude how thorough to be, and which specialists to involve.
                </p>
              </div>

              <!-- Modes -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Three Modes</p>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                  {[
                    { name: "FAST", default: true, when: "Default for small fixes (Tier 0–2). Diff-only output. Terse responses.",
                      example: "Fix a typo · Tweak spacing · Add a CTA · Update a colour" },
                    { name: "SHIP", default: false, when: 'Triggered by words like "ship", "finalise", "review", "launch", "production". Runs the full agent pipeline.',
                      example: "Build the homepage · Launch the site · Final review before going live" },
                    { name: "EXPLORE", default: false, when: "Questions, lookups, read-only investigation. No agents called. Terse answers.",
                      example: "Where is X defined? · How does cookie consent work? · What does this function do?" },
                  ].map(m => (
                    <div class="p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex flex-col gap-2">
                      <div class="flex items-center gap-2 flex-wrap">
                        <span class="text-sm font-bold text-[--color-headline]">{m.name}</span>
                        {m.default && <span class="text-[10px] font-semibold text-[--color-primary] bg-[--color-accent] px-2 py-0.5 rounded-[--radius-badge] uppercase tracking-wide">Default</span>}
                      </div>
                      <p class="m-0 text-xs text-[--color-body] leading-relaxed">{m.when}</p>
                      <p class="m-0 text-xs text-[--color-caption] italic">e.g. {m.example}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Tiers -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Six Tiers (size of the job)</p>
                <div class="flex flex-col gap-2">
                  {[
                    { tier: "Tier 0", title: "Question / lookup", agents: "None — answer directly", trigger: '"explain", "what is", "where is", "show me"' },
                    { tier: "Tier 1", title: "Single-domain fix", agents: "1 specialist agent", trigger: '"fix the H1", "add alt text", "check the CSP"' },
                    { tier: "Tier 2", title: "Visual change with no UX planning", agents: "ui-designer → frontend-builder", trigger: '"add a card", "restyle the button", "fix mobile spacing"' },
                    { tier: "Tier 3", title: "New component or feature", agents: "ux-architect → ui-designer → frontend-builder + reviewer(s)", trigger: '"build a new section", "add a contact form", "interactive widget"' },
                    { tier: "Tier 4", title: "New page or major redesign", agents: "Full IA → UX → UI → build → 5+ reviewers", trigger: '"build the contact page", "redesign the services page"' },
                    { tier: "Tier 5", title: "Homepage / template / structural change", agents: "All 12 agents in sequence", trigger: '"build the homepage", "set up a new client", "rebuild the template"' },
                  ].map(t => (
                    <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-14">{t.tier}</span>
                      <div class="flex-1 min-w-[12rem] flex flex-col gap-1">
                        <p class="m-0 text-sm font-semibold text-[--color-headline]">{t.title}</p>
                        <p class="m-0 text-xs text-[--color-body]">{t.agents}</p>
                        <p class="m-0 text-xs text-[--color-caption] italic">Trigger words: {t.trigger}</p>
                      </div>
                    </div>
                  ))}
                </div>
                <p class="m-0 mt-2 text-xs text-[--color-caption] italic">When in doubt, escalate one tier higher.</p>
              </div>

              <!-- Announcement rule -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Announcement Rule</p>
                <p class="m-0 text-sm text-[--color-body]">For Tier 2 and above, Claude must say which agents will run BEFORE doing the work. Example:</p>
                <div class="p-3 bg-[--color-headline] rounded-[--radius-card]">
                  <p class="m-0 font-mono text-xs text-[#86efac] leading-relaxed">
                    Pipeline: ui-designer → frontend-builder
                  </p>
                </div>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               18 — SKILLS LIBRARY
          ─────────────────────────────────────────────────────── -->
          <article id="skills" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">19</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Skills Library</h5>
                  <p class="text-sm text-[--color-caption] m-0">Reusable build patterns. Each skill is a step-by-step recipe Claude follows when the work matches.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Skills</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> A "skill" is a written recipe. When Claude is asked to build something we've solved before, it loads the matching recipe instead of reinventing the work. All skills live in <code class="code-label">.claude/skills/</code> and are read on demand.
                </p>
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {[
                  { num: "01", file: "contactform.md", title: "Web3Forms Contact Form",
                    when: "Building any contact / enquiry / booking form",
                    summary: "Direct-to-Web3Forms POST with reCAPTCHA v3, honeypot, replyto, ccemail (semicolon!), recaptcha_response (NOT g-recaptcha-response), isSubmitting guard. Production-tested. Replaces every contact form pattern." },
                  { num: "02", file: "hero-video.md", title: "Hero Video — MANDATORY MP4 H.264",
                    when: "Adding any hero or background video",
                    summary: "Self-hosted MP4 H.264 only. Never YouTube, never WebM-only. JS-injected source for Safari safety. Mobile + reduced-motion get poster only — zero video bytes. Use npm run optimise-video." },
                  { num: "03", file: "performance.md", title: "Site-Wide Performance",
                    when: "Cloning the template / pre-launch / Lighthouse audit",
                    summary: "Cloudflare adapter ASSETS patch, deferred GTM loader, dual hero preload, 1-year cache headers, robots ASCII rule, _redirects status codes, Zaraz migration plan." },
                  { num: "04", file: "carousel.md", title: "Swiper Carousel",
                    when: "Building any carousel / slider",
                    summary: "Swiper.js implementation pattern. Imports only the modules in use. CLS-safe — reserves height before JS hydration." },
                  { num: "05", file: "header-menu.md", title: "Header Navigation",
                    when: "Building or editing site navigation",
                    summary: "Sticky header pattern, dropdown / flyout structure, active-state helpers (isActive / isSection), mobile drawer." },
                  { num: "06", file: "googlereviews-carousel.md", title: "Google Reviews Carousel",
                    when: "Adding the global pre-footer reviews section / updating reviews per client",
                    summary: "Free, manual Trustindex-style pattern. Native Astro carousel (~600 bytes JS), auto-filtered to 4–5 stars, capped at 20, newest first. Dynamic LocalBusiness + AggregateRating + Review[] JSON-LD. Optional Google Places API auto-fetch." },
                ].map(s => (
                  <div class="flex flex-col gap-2 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="flex items-center gap-2 flex-wrap">
                      <span class="text-xs font-mono font-bold text-[--color-primary]">{s.num}</span>
                      <code class="code-label text-[0.7rem] leading-tight">.claude/skills/{s.file}</code>
                    </div>
                    <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                    <p class="m-0 text-[10px] font-semibold text-[--color-primary] uppercase tracking-wide">USE WHEN: {s.when}</p>
                    <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.summary}</p>
                  </div>
                ))}
              </div>

              <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                <p class="m-0 text-xs text-[--color-caption]">
                  <strong>How to invoke a skill:</strong> say something like <em>"use the contactform skill"</em> or <em>"build the hero with the hero-video pattern"</em>. Claude reads the skill file and follows it exactly.
                </p>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               19 — PERFORMANCE & BUILD TOOLING
          ─────────────────────────────────────────────────────── -->
          <article id="perfbuild" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">20</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Performance & Build Tooling</h5>
                  <p class="text-sm text-[--color-caption] m-0">The site-wide setup that ships every clone with PageSpeed-friendly defaults.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Performance</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Every clinic site has the same speed problems by default — slow images, heavy tracking scripts, deploy errors. The template solves all of them up front so each new client starts at 90+ on PageSpeed without bespoke work.
                </p>
              </div>

              <!-- 1. Cloudflare adapter patch -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">1. Cloudflare adapter patch (deploy-blocker fix)</p>
                <p class="m-0 text-sm text-[--color-body]">
                  Cloudflare Pages refuses to deploy if any binding is named <code class="code-label">ASSETS</code> (it's reserved). The Astro adapter hardcodes that name in 5 files. Our patch script renames every occurrence to <code class="code-label">STATIC_ASSETS</code> automatically after every install.
                </p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-xs text-[--color-body]"><code class="code-label">scripts/patch-cloudflare-adapter.mjs</code> — wired to <code class="code-label">npm postinstall</code>. Idempotent (safe to re-run).</p>
                </div>
              </div>

              <!-- 2. Image scripts -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">2. Image optimisation</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm text-[--color-headline]">npm run resize-images</strong> <span class="text-xs text-[--color-caption]">— resizes any oversized .webp to a max dimension (default 1920px), in-place re-encode at quality 82 using sharp.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm text-[--color-headline]">Dual hero preload</strong> <span class="text-xs text-[--color-caption]">— BaseLayout accepts both <code class="code-label">heroImage</code> (desktop) and <code class="code-label">heroImageMobile</code>. Each device only fetches the variant it paints.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm text-[--color-headline]">All images .webp</strong> <span class="text-xs text-[--color-caption]">— with explicit width/height, decoding="async", lazy by default, eager only for above-the-fold heroes.</span></li>
                </ul>
              </div>

              <!-- 3. Cache headers -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">3. Cache headers (public/_headers)</p>
                <p class="m-0 text-sm text-[--color-body]">Cloudflare Pages auto-purges on every deploy, so 1-year caches are safe even on non-content-hashed paths.</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                  {[
                    { path: "/_astro/*", ttl: "1 year, immutable" },
                    { path: "/images/*", ttl: "1 year + stale-while-revalidate" },
                    { path: "/videos/*", ttl: "1 year + stale-while-revalidate" },
                    { path: "/fonts/*",  ttl: "1 year, immutable" },
                    { path: "/icons/*",  ttl: "1 year, immutable" },
                    { path: "/*.svg",    ttl: "7 days" },
                  ].map(c => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex items-center gap-3 flex-wrap">
                      <code class="code-label shrink-0">{c.path}</code>
                      <span class="text-xs text-[--color-body]">{c.ttl}</span>
                    </div>
                  ))}
                </div>
              </div>

              <!-- 4. Deferred GTM -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">4. Deferred GTM loader</p>
                <p class="m-0 text-sm text-[--color-body]">GTM saves 150–220 KB of JS by loading on idle, first user interaction, or 6-second fallback (Safari). Final move post-launch: migrate to Cloudflare Zaraz (zero browser JS).</p>
              </div>

              <!-- 5. Site-wide rules -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">5. Site-wide rules</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">&lt;html lang="en-GB"&gt;</code> <span class="text-xs text-[--color-caption]">— UK targeting, British screen-reader voice, British spellcheck.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm">noindex default = true</strong> <span class="text-xs text-[--color-caption]">— template ships in noindex mode. Flip to false ONLY when the user says "go live".</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm">robots.txt is ASCII-only</strong> <span class="text-xs text-[--color-caption]">— Cloudflare serves it without charset; box-drawing characters cause mojibake.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm">_redirects status codes</strong> <span class="text-xs text-[--color-caption]">— only 200/301/302/303/307/308 work on Pages. 404/410 need a Pages Function.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><strong class="text-sm">CSP includes media-src 'self'</strong> <span class="text-xs text-[--color-caption]">— required for self-hosted hero video.</span></li>
                </ul>
              </div>

              <!-- 6. Targets -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">6. Core Web Vitals targets</p>
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                  {[
                    { metric: "LCP", target: "< 2.5s", note: "Largest Contentful Paint (hero image load)" },
                    { metric: "CLS", target: "< 0.1",  note: "Layout Shift (no jumping content)" },
                    { metric: "INP", target: "< 200ms", note: "Interaction responsiveness" },
                  ].map(m => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <p class="m-0 text-xs font-bold text-[--color-primary] uppercase tracking-wide">{m.metric}</p>
                      <p class="m-0 mt-1 text-lg font-bold text-[--color-headline]">{m.target}</p>
                      <p class="m-0 text-[11px] text-[--color-caption]">{m.note}</p>
                    </div>
                  ))}
                </div>
                <p class="m-0 text-xs text-[--color-caption]">PageSpeed mobile target: <strong>90+</strong>. Below that on a fresh clone with no client-specific tuning means something has regressed in the template.</p>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               20 — HERO VIDEO PATTERN (MANDATORY)
          ─────────────────────────────────────────────────────── -->
          <article id="herovideo" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">21</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Hero Video Pattern</h5>
                  <p class="text-sm text-[--color-caption] m-0">Self-hosted MP4 H.264. Mandatory across every HMDG clinic site.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-white bg-[--color-primary] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">MANDATORY</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- The big rule -->
              <div class="p-4 bg-[--color-primary]/8 border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm font-bold text-[--color-headline]">The Rule (no exceptions without sign-off):</p>
                <ul class="list-none p-0 m-0 mt-2 flex flex-col gap-1">
                  <li class="text-sm text-[--color-body]">✓ Self-hosted <strong>MP4 H.264</strong></li>
                  <li class="text-sm text-[--color-body]">✗ NEVER YouTube embed</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER Vimeo embed</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER WebM-only (Safari needs MP4)</li>
                </ul>
              </div>

              <!-- Why -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Why</p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">YouTube embed</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">~1500ms CPU + ~1MB third-party JS. PageSpeed kills you.</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">Self-hosted MP4 H.264</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">~720KB single file + 0ms third-party CPU. Safari uses OS hardware decoder.</p>
                  </div>
                </div>
                <p class="m-0 text-xs text-[--color-caption] italic">Note: Safari has supported MP4 natively since 2007. WebM is for Chrome/Firefox optimisation, NOT a Safari fallback. If you ship one format, ship MP4.</p>
              </div>

              <!-- File layout -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">File layout</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">assets/videos/herovideo.mp4</code> <span class="text-xs text-[--color-caption]">— SOURCE master. Outside public/, never ships to production.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">public/videos/hero-loop.mp4</code> <span class="text-xs text-[--color-caption]">— ENCODED output. Committed and deployed (~700KB–1.5MB).</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">public/images/herofallback.webp</code> <span class="text-xs text-[--color-caption]">— POSTER. Always required, shown to mobile + reduced-motion users.</span></li>
                </ul>
              </div>

              <!-- Steps -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step-by-step</p>
                {[
                  { num: "1", title: "Drop the source video", body: "Save the source video to assets/videos/herovideo.mp4. Any container/codec is fine — the script handles conversion." },
                  { num: "2", title: "Encode it", body: "Run npm run optimise-video. Output lands at public/videos/hero-loop.mp4 — capped at 1280×720, 10 seconds, audio stripped, optimised for streaming." },
                  { num: "3", title: "Use it on a page", body: "Pass image (poster) and video (encoded MP4) props to <Hero />. Component handles the JS-injected source pattern automatically." },
                  { num: "4", title: "Verify", body: 'Open in Safari + Chrome desktop (plays). Open in mobile (poster only — no video bytes). Toggle "Reduce motion" (poster only).' },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <span class="text-lg font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{s.num}</span>
                    <div class="flex-1">
                      <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                      <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                    </div>
                  </div>
                ))}
              </div>

              <!-- Encode spec (for tech reviewers) -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Encode spec (non-negotiable)</p>
                <pre class="code-block"><code>{`-crf 28              quality target
-preset slow         better compression
-pix_fmt yuv420p     SAFARI MUST HAVE THIS
-movflags +faststart streaming-friendly
-an                  strip audio
-t 10                trim to 10 seconds
-vf scale=':min(1280,iw):-2'   cap width at 1280px`}</code></pre>
                <p class="m-0 text-xs text-[--color-caption]">Full skill reference: <code class="code-label">.claude/skills/hero-video.md</code></p>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               21 — PROJECT LOG PROTOCOL
          ─────────────────────────────────────────────────────── -->
          <article id="projectlog" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">22</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Project Log Protocol</h5>
                  <p class="text-sm text-[--color-caption] m-0">A simple internal log so every Claude session can pick up where the last one left off.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Workflow</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Imagine a notebook on the project where every working day someone writes "here's what I did, here's what's still open, here's what to do next." That's the project log. Claude reads it before starting and updates it before finishing. No more cold-starts.
                </p>
              </div>

              <!-- Where -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Where</p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body]"><code class="code-label">.project-log.md</code> at the project root. Committed to GitHub. <strong>Never deployed</strong> — sits outside <code class="code-label">src/</code> and <code class="code-label">public/</code>, so neither Astro nor Cloudflare Pages can serve it.</p>
                </div>
              </div>

              <!-- Rules -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Rules</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2 counter-reset:rule">
                  {[
                    { title: "Read first", body: "Before starting any task, read the log. Current Priorities + Unfinished + Blocked describe the running state." },
                    { title: "Update last", body: "Before declaring complete (or ending the session), append a Change Log entry and reconcile the Unfinished list." },
                    { title: "Always write down unfinished work", body: "If a task isn't fully shipped (uncommitted code, missing assets, follow-ups) it MUST live in Unfinished / To Do with enough detail to resume cold." },
                    { title: "Every entry has 5 fields", body: "Date · Status (completed / in progress / blocked / to do) · Task summary · Files changed · Next action. Notes optional." },
                    { title: "Keep it lightweight", body: "Short factual entries. No essays, no screenshots, no decoration." },
                  ].map((r, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-6">{String(i + 1).padStart(2, "0")}</span>
                      <div class="flex-1">
                        <p class="m-0 text-sm font-semibold text-[--color-headline]">{r.title}</p>
                        <p class="m-0 text-xs text-[--color-body] leading-relaxed">{r.body}</p>
                      </div>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Sections -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">What's in the log</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                  {[
                    { sec: "Current Priorities", desc: "What we're focused on right now" },
                    { sec: "Unfinished / To Do", desc: "Anything not fully shipped — must live here, not in someone's head" },
                    { sec: "Blocked", desc: "Anything waiting on external info or decisions" },
                    { sec: "Change Log", desc: "Dated entries — what changed, what files, what's next" },
                    { sec: "Session Notes", desc: "Useful context, gotchas, decisions that affect later work" },
                  ].map(s => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <p class="m-0 text-xs font-bold text-[--color-headline]">{s.sec}</p>
                      <p class="m-0 mt-0.5 text-xs text-[--color-body]">{s.desc}</p>
                    </div>
                  ))}
                </div>
              </div>

              <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                <p class="m-0 text-xs text-[--color-caption]">
                  <strong>How is this different from CHANGELOG.md?</strong> CHANGELOG.md is the public semver release history of the template. <code class="code-label">.project-log.md</code> is the internal working notebook. CHANGELOG ships with the template; <code class="code-label">.project-log.md</code> stays internal.
                </p>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               22 — ENVIRONMENT VARIABLES
          ─────────────────────────────────────────────────────── -->
          <article id="envvars" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">23</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Environment Variables</h5>
                  <p class="text-sm text-[--color-caption] m-0">12 variables that drive every deploy. Most come from the marketing team.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Config</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Environment variables are settings that change per client without changing the code. Things like the clinic name, the contact email, the analytics ID. They live in a file called <code class="code-label">.env</code> locally and in Cloudflare Pages settings on production. The template ships with <code class="code-label">.env.example</code> showing every variable and what it's for.
                </p>
              </div>

              <!-- Glossary -->
              <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                <p class="m-0 text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-1">Glossary</p>
                <p class="m-0 text-xs text-[--color-body]"><strong>PUBLIC_ prefix</strong> = Astro convention. Variables starting with <code class="code-label">PUBLIC_</code> are baked into the HTML and visible in browser. Use only for values that are designed to be public (Web3Forms key, GA4 ID, recaptcha site key). Variables WITHOUT <code class="code-label">PUBLIC_</code> stay server-side only.</p>
              </div>

              <!-- Browser-exposed table -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Table 1 — Browser-exposed (PUBLIC_*)</p>
                <p class="m-0 text-xs text-[--color-caption] italic">Baked at build time. Visible in HTML source. Cloudflare needs a redeploy after changing these.</p>
                <div class="overflow-x-auto">
                  <table class="w-full border border-[--color-border] rounded-[--radius-card] overflow-hidden text-xs">
                    <thead class="bg-[--color-muted]">
                      <tr>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Variable</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Source</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Purpose</th>
                      </tr>
                    </thead>
                    <tbody>
                      {[
                        { v: "PUBLIC_SITE_NAME",          src: "Marketing", p: "Email subject + from_name + page titles. e.g. \"Smith Clinic\"" },
                        { v: "PUBLIC_SITE_URL",           src: "Dev",       p: "Optional canonical URL fallback. Prefer site: in astro.config.mjs." },
                        { v: "PUBLIC_GA_MEASUREMENT_ID",  src: "Marketing", p: "GA4 measurement ID, e.g. G-XXXXXXXXXX" },
                        { v: "PUBLIC_GOOGLE_MAPS_API_KEY",src: "Marketing", p: "Google Maps embed (optional)" },
                        { v: "PUBLIC_CLINIKO_BOOKING_URL",src: "Marketing", p: "Cliniko booking widget URL (optional)" },
                        { v: "PUBLIC_WEB3FORMS_KEY",      src: "Marketing", p: "Web3Forms access UUID. Pro plan required." },
                        { v: "PUBLIC_CLIENT_EMAIL",       src: "Marketing", p: "CC list for the contact form. SEMICOLON-separated, NOT comma." },
                        { v: "PUBLIC_RECAPTCHA_SITE_KEY", src: "Marketing", p: "reCAPTCHA v3 Site Key (browser). Secret Key goes in Web3Forms dashboard." },
                      ].map(r => (
                        <tr class="border-t border-[--color-border] bg-white">
                          <td class="px-3 py-2 align-top"><code class="code-label text-[10px]">{r.v}</code></td>
                          <td class="px-3 py-2 align-top whitespace-nowrap">
                            <span class={`text-[10px] font-semibold uppercase tracking-wide px-2 py-0.5 rounded-[--radius-badge] ${r.src === "Marketing" ? "text-[--color-primary] bg-[--color-accent]" : "text-[--color-caption] bg-[--color-muted]"}`}>{r.src}</span>
                          </td>
                          <td class="px-3 py-2 align-top text-[--color-body] leading-relaxed">{r.p}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- Server-side table -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Table 2 — Server-side (no PUBLIC_ prefix)</p>
                <p class="m-0 text-xs text-[--color-caption] italic">Never exposed to the browser. Used by Cloudflare Pages Functions (API routes) only.</p>
                <div class="overflow-x-auto">
                  <table class="w-full border border-[--color-border] rounded-[--radius-card] overflow-hidden text-xs">
                    <thead class="bg-[--color-muted]">
                      <tr>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Variable</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Source</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Purpose</th>
                      </tr>
                    </thead>
                    <tbody>
                      {[
                        { v: "CONTACT_EMAIL",       src: "Marketing", p: "Server-side fallback contact email. Never appears in HTML." },
                        { v: "GA4_API_SECRET",      src: "Marketing", p: "Measurement Protocol API secret. GA4 → Admin → Data Streams." },
                        { v: "GA4_MEASUREMENT_ID",  src: "Marketing", p: "Server-side GA4 ID. Should match PUBLIC_GA_MEASUREMENT_ID." },
                        { v: "SITE_ORIGIN",         src: "Dev",       p: "Production URL. Used by API routes to validate request origin." },
                      ].map(r => (
                        <tr class="border-t border-[--color-border] bg-white">
                          <td class="px-3 py-2 align-top"><code class="code-label text-[10px]">{r.v}</code></td>
                          <td class="px-3 py-2 align-top whitespace-nowrap">
                            <span class={`text-[10px] font-semibold uppercase tracking-wide px-2 py-0.5 rounded-[--radius-badge] ${r.src === "Marketing" ? "text-[--color-primary] bg-[--color-accent]" : "text-[--color-caption] bg-[--color-muted]"}`}>{r.src}</span>
                          </td>
                          <td class="px-3 py-2 align-top text-[--color-body] leading-relaxed">{r.p}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- How to set them -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">How to set them — step by step</p>
                {[
                  { num: "1", title: "Local development", body: "Copy .env.example to .env in the project root. Fill in real values. NEVER commit .env (it's git-ignored)." },
                  { num: "2", title: "Production — Cloudflare Pages", body: "Cloudflare dashboard → Workers & Pages → your project → Settings → Variables and Secrets. Add each variable under the BUILD scope (NOT Runtime — these bake at build time). Type: Plaintext." },
                  { num: "3", title: "Trigger a redeploy", body: "Cloudflare does NOT auto-rebuild when env vars change. Go to Deployments → latest deployment → Retry deployment. Without this, the form keeps using the old build's values." },
                  { num: "4", title: "Verify it worked", body: "After redeploy, view-source on the contact page and search for the value. If you see your real values rendered, you're done." },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <span class="text-lg font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{s.num}</span>
                    <div class="flex-1">
                      <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                      <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                    </div>
                  </div>
                ))}
              </div>

              <!-- Critical notes -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Critical notes</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] text-xs text-[--color-body]"><strong>Web3Forms ccemail uses semicolons.</strong> Example: <code class="code-label">client@x.co.uk; agency@hmdg.co.uk</code> — the template auto-normalises commas to semicolons but new values should use semicolons directly.</li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] text-xs text-[--color-body]"><strong>reCAPTCHA Secret Key never goes in .env.</strong> It lives in the Web3Forms dashboard under Spam Protection → reCAPTCHA. Site Key is public (PUBLIC_); Secret Key is private (Web3Forms holds it).</li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] text-xs text-[--color-body]"><strong>PUBLIC_ vars are build-time, not runtime.</strong> Editing them in Cloudflare without redeploying changes nothing. Always Retry deployment.</li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] text-xs text-[--color-body]"><strong>Don't put .env in src/, public/, or anywhere it could be served.</strong> The default location (project root) is correct.</li>
                </ul>
              </div>

            </div>

            <div class="bg-white px-8 pb-8">
              <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — Set Up Env Vars</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">Set up environment variables for [CLIENT NAME]. Copy .env.example to .env locally and fill in real values for the 12 variables (PUBLIC_SITE_NAME, PUBLIC_SITE_URL, PUBLIC_GA_MEASUREMENT_ID, PUBLIC_GOOGLE_MAPS_API_KEY, PUBLIC_CLINIKO_BOOKING_URL, PUBLIC_WEB3FORMS_KEY, PUBLIC_CLIENT_EMAIL with semicolons, PUBLIC_RECAPTCHA_SITE_KEY, CONTACT_EMAIL, GA4_API_SECRET, GA4_MEASUREMENT_ID, SITE_ORIGIN). Then add the same values to Cloudflare Pages → Settings → Variables and Secrets → Build scope. Click Retry deployment when done. Verify by curling the contact page and grepping for the rendered values.</p>
                </div>
              </div>
            </div>

          </article>



          <!-- ───────────────────────────────────────────────────
               23 — BASELAYOUT (was 17)
          ─────────────────────────────────────────────────────── -->
          <article id="baselayout" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">24</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">BaseLayout.astro</h5>
                  <p class="text-sm text-[--color-caption] m-0">The single shared wrapper for every client-facing page.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Layout</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- Props -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Props</p>
                {[
                  { prop: "title",       type: "string",   required: true,  desc: "Page title tag content. Required on every page." },
                  { prop: "description", type: "string?",  required: false, desc: "Meta description for SEO. Defaults to a generic clinic description if omitted." },
                  { prop: "noindex",     type: "boolean?", required: false, desc: "Set true on thank-you and booking confirmation pages. Injects a noindex, nofollow robots meta tag in the page head." },
                ].map(p => (
                  <div class="flex items-start gap-4 p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                    <code class="code-label shrink-0">{p.prop}</code>
                    <code class="code-hex shrink-0">{p.type}</code>
                    {p.required && <span class="text-xs font-bold text-[--color-primary] bg-[--color-accent] border border-[--color-primary]/20 px-2 py-0.5 rounded-[--radius-badge] shrink-0">required</span>}
                    <p class="m-0 text-sm text-[--color-body]">{p.desc}</p>
                  </div>
                ))}
              </div>

              <!-- What BaseLayout includes automatically -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Included Automatically — Do Not Re-Import</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { item: "global.css",          note: "Once globally — never import again per-page" },
                    { item: "Google Fonts",         note: "Inter + Inter Tight, weights 400–800" },
                    { item: "Security meta tags",   note: "CSP, X-Frame, referrer — plus _headers / vercel.json" },
                    { item: "Consent Mode v2",      note: "Fires before GTM — required for GDPR compliance" },
                    { item: "GTM noscript",         note: "Fallback tag — only active when gtmId is set in config" },
                    { item: "Header component",     note: "Global — never add Header inside a page" },
                    { item: "Footer component",     note: "Global — never add Footer inside a page" },
                    { item: "CookieConsent",        note: "Global — never add CookieConsent inside a page" },
                  ].map(i => (
                    <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <div class="flex flex-col gap-0.5">
                        <code class="code-label">{i.item}</code>
                        <p class="m-0 text-xs text-[--color-caption]">{i.note}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Usage pattern -->
              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">Usage Pattern</p>
                <pre class="code-block"><code>---
import BaseLayout from '../layouts/BaseLayout.astro';
---

&lt;BaseLayout
  title="Services — ClinicName"
  description="Expert physiotherapy in London. Book online today."
&gt;
  &lt;section class="bg-[--color-muted]"&gt;
    &lt;div class="container-main"&gt;
      &lt;div class="container-inner"&gt;
        ...page content...
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/section&gt;
&lt;/BaseLayout&gt;

/* For thank-you and booking confirmation pages — add noindex */
&lt;BaseLayout title="Thank You" description="..." noindex=&lbrace;true&rbrace;&gt;</code></pre>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — BaseLayout</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Build a new page at [URL]. Use BaseLayout from src/layouts/BaseLayout.astro. Pass title and description props. Never import global.css, Header, Footer, or CookieConsent separately — BaseLayout handles all of these. Content goes directly inside the BaseLayout tags. If this is a thank-you or confirmation page, add noindex prop set to true on the BaseLayout.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               18 — COOKIE CONSENT CONFIG
          ─────────────────────────────────────────────────────── -->
          <article id="cookiecfg" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">25</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Cookie Consent Config</h5>
                  <p class="text-sm text-[--color-caption] m-0">Tracking IDs are env-driven. Marketing team manages banner copy + policy version in <code class="code-label">src/config/cookie-consent.config.ts</code>.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Config</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- Plain English -->
              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Tracking IDs (GTM, GA4, Google Ads) used to live as hardcoded strings in the config file. They now live in <code class="code-label">.env</code> and are read at build time. The config file still controls banner copy, policy version, and the booking platform list — but never tracking IDs again.
                </p>
              </div>

              <!-- The Big Rule — REQUIRED env vars for go-live -->
              <div class="p-4 bg-[--color-primary]/8 border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm font-bold text-[--color-headline]">Go-live blocker — these env vars MUST be set:</p>
                <ul class="list-none p-0 m-0 mt-2 flex flex-col gap-1">
                  <li class="text-sm text-[--color-body]">✓ <code class="code-label">PUBLIC_GTM_ID</code> <strong>OR</strong> <code class="code-label">PUBLIC_GTAG_ID</code> — at least one (GTM is preferred when both modes are available)</li>
                  <li class="text-sm text-[--color-body]">✓ <code class="code-label">GA4_MEASUREMENT_ID</code> + <code class="code-label">GA4_API_SECRET</code> — both required for server-side booking events</li>
                  <li class="text-sm text-[--color-body]">✓ <code class="code-label">PUBLIC_SITE_NAME</code> — banner copy reads <code class="code-label">{`At \${PUBLIC_SITE_NAME}, we use cookies…`}</code></li>
                  <li class="text-sm text-[--color-body]">✓ <code class="code-label">SITE_ORIGIN</code> — API endpoints reject requests from any other origin</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER hardcode tracking IDs in <code class="code-label">cookie-consent.config.ts</code> again</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER commit a real <code class="code-label">.env</code> to git (it's gitignored — keep it that way)</li>
                </ul>
              </div>

              <!-- REQUIRED env vars -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Required for go-live</p>
                <p class="m-0 text-xs text-[--color-caption] italic">Set every variable below in <code class="code-label">.env</code> locally AND in Cloudflare Pages → Settings → Variables → Build scope. Click "Retry deployment" after saving.</p>
                {[
                  { v: "PUBLIC_GTM_ID",          ex: "GTM-XXXXXXX",       desc: "Google Tag Manager container ID. Preferred mode (GTM-only). Find: GTM → Admin → Container Settings." },
                  { v: "PUBLIC_GTAG_ID",         ex: "G-XXXXXXXXXX",      desc: "GA4 Measurement ID for direct gtag.js mode (no GTM). Use ONLY if PUBLIC_GTM_ID is blank. Find: GA4 → Admin → Data Streams → Web stream." },
                  { v: "PUBLIC_GA_MEASUREMENT_ID", ex: "G-XXXXXXXXXX",    desc: "Browser-side GA4 ID — legacy fallback for components that read it directly. Match it to PUBLIC_GTAG_ID." },
                  { v: "GA4_MEASUREMENT_ID",     ex: "G-XXXXXXXXXX",      desc: "Server-side GA4 ID. Used by /api/book-now + /api/booking-complete to forward Measurement Protocol events. NEVER use the PUBLIC_ prefix here." },
                  { v: "GA4_API_SECRET",         ex: "your-api-secret",   desc: "GA4 Measurement Protocol API Secret. Find: GA4 → Admin → Data Streams → select stream → Measurement Protocol API secrets. Server-side only — never PUBLIC_." },
                  { v: "PUBLIC_SITE_NAME",       ex: "Your Clinic Name",  desc: "Drives the cookie banner copy (\"At {clinic}, we use cookies…\"), email from_name, page titles, JSON-LD." },
                  { v: "SITE_ORIGIN",            ex: "https://yourclinic.co.uk", desc: "Production URL with https:// and no trailing slash. API routes reject requests whose Origin header doesn't match." },
                ].map(r => (
                  <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                    <code class="code-label shrink-0">{r.v}</code>
                    <span class="text-[10px] font-semibold uppercase tracking-wide text-white bg-[--color-primary] px-2 py-0.5 rounded-[--radius-badge] shrink-0">Required</span>
                    <code class="code-hex shrink-0 text-[10px]">{r.ex}</code>
                    <p class="m-0 text-xs text-[--color-body] flex-1 min-w-[12rem]">{r.desc}</p>
                  </div>
                ))}
              </div>

              <!-- OPTIONAL env vars -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Optional</p>
                <p class="m-0 text-xs text-[--color-caption] italic">Site works without these — leave blank to disable the named feature or accept the default.</p>
                {[
                  { v: "PUBLIC_ADS_CONVERSION_ID",        ex: "AW-XXXXXXXXX", desc: "Google Ads Conversion ID — only needed for Google Ads remarketing / conversion tracking. Find: Google Ads → Tools → Tag Settings." },
                  { v: "PUBLIC_SERVER_SIDE_TRACKING",     ex: "true | false", desc: "Master switch. Set 'false' to disable /api/book-now + /api/booking-complete (e.g. staging or privacy-mode). Default: true." },
                  { v: "PUBLIC_BOOKING_DISABLE",          ex: "acuity,timely", desc: "Disable specific booking platforms (comma-separated, lowercase keys: acuity, calendly, cliniko, jane, phorest, pracsuite, simplybook, timely, youcanbook). Default: ALL enabled." },
                  { v: "PUBLIC_BOOKING_ADDITIONAL_DOMAINS", ex: "mybooking.co.uk", desc: "Add custom booking domains not in the catalog (comma-separated). Example: white-label Cliniko subdomain." },
                ].map(r => (
                  <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                    <code class="code-label shrink-0">{r.v}</code>
                    <span class="text-[10px] font-semibold uppercase tracking-wide text-[--color-caption] bg-white border border-[--color-border] px-2 py-0.5 rounded-[--radius-badge] shrink-0">Optional</span>
                    <code class="code-hex shrink-0 text-[10px]">{r.ex}</code>
                    <p class="m-0 text-xs text-[--color-body] flex-1 min-w-[12rem]">{r.desc}</p>
                  </div>
                ))}
              </div>

              <!-- Marketing team — non-tracking fields -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Marketing team — edit in <code class="code-label">cookie-consent.config.ts</code></p>
                <p class="m-0 text-xs text-[--color-caption] italic">Tracking IDs are env-driven (above). The fields below remain in the config file because they are content, not credentials.</p>
                {[
                  { field: "policyVersion",    example: "'1.0'",             desc: "Start every new site at '1.0'. Bumping this forces ALL existing visitors to re-consent. Only change when the cookie policy content actually changes." },
                  { field: "bannerTitle",      example: "'We use cookies'",  desc: "Heading shown in the cookie consent banner." },
                  { field: "bannerText",       example: "auto from PUBLIC_SITE_NAME", desc: "Body text in the banner. Auto-renders \"At {PUBLIC_SITE_NAME}, we use cookies…\" — only edit if the client wants different copy." },
                  { field: "acceptAllLabel",   example: "'Accept All'",      desc: "Primary accept button label. UK English spelling required." },
                  { field: "rejectAllLabel",   example: "'Reject All'",      desc: "Reject button label. UK English spelling required." },
                  { field: "customiseLabel",   example: "'Customise'",       desc: "Settings button label. Note UK spelling — Customise not Customize." },
                ].map(f => (
                  <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                    <code class="code-label shrink-0">{f.field}</code>
                    <code class="code-hex shrink-0 text-[10px]">{f.example}</code>
                    <p class="m-0 text-xs text-[--color-body] flex-1 min-w-[12rem]">{f.desc}</p>
                  </div>
                ))}
              </div>

              <!-- Code example — env-driven -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Config file — env-driven shape</p>
                <pre class="code-block"><code>{`// src/config/cookie-consent.config.ts
const ENV_GTM_ID            = import.meta.env.PUBLIC_GTM_ID            || '';
const ENV_GTAG_ID           = import.meta.env.PUBLIC_GTAG_ID           || '';
const ENV_ADS_CONVERSION_ID = import.meta.env.PUBLIC_ADS_CONVERSION_ID || '';
const ENV_SERVER_SIDE       = (import.meta.env.PUBLIC_SERVER_SIDE_TRACKING || 'true').toLowerCase() !== 'false';
const ENV_SITE_NAME         = import.meta.env.PUBLIC_SITE_NAME || 'this clinic';

export const cookieConsentConfig = {
  gtmId:              ENV_GTM_ID,             // ← env-driven
  gtagId:             ENV_GTAG_ID,            // ← env-driven
  adsConversionId:    ENV_ADS_CONVERSION_ID,  // ← env-driven (optional)
  serverSideTracking: ENV_SERVER_SIDE,        // ← env-driven (default true)
  policyVersion:      '1.0',                  // ← bump only when policy changes
  bannerTitle:        'We use cookies',
  bannerText:         \`At \${ENV_SITE_NAME}, we use cookies…\`,
  acceptAllLabel:     'Accept All',
  rejectAllLabel:     'Reject All',
  customiseLabel:     'Customise',
  // ...booking platforms (env-overridable)
};`}</code></pre>
              </div>

              <!-- Important rules -->
              <div class="p-4 bg-[--color-accent] border border-[--color-primary]/20 rounded-[--radius-card] flex gap-3 items-start">
                <span class="text-[--color-primary] font-bold text-sm shrink-0">⚠</span>
                <div class="flex flex-col gap-1">
                  <p class="m-0 text-sm font-semibold text-[--color-headline]">Important rules</p>
                  <ul class="m-0 text-sm text-[--color-body] flex flex-col gap-1 pl-4">
                    <li>Tracking IDs (<code class="code-label">PUBLIC_GTM_ID</code>, <code class="code-label">PUBLIC_GTAG_ID</code>, <code class="code-label">GA4_*</code>) live in <code class="code-label">.env</code> only — never hardcoded in the config file.</li>
                    <li><code class="code-label">PUBLIC_*</code> vars are baked at build time. Cloudflare Pages requires a Retry deployment after changing them.</li>
                    <li>Only bump <code class="code-label">policyVersion</code> when the cookie policy page text actually changes — not on every deploy.</li>
                    <li>All button labels must use UK English spelling (Customise, not Customize).</li>
                    <li>The <code class="code-label">bannerText</code> must reflect what the live <a href="/cookie-policy" class="text-[--color-primary]">/cookie-policy</a> page says.</li>
                  </ul>
                </div>
              </div>

              <!-- Go-live checklist -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Go-live checklist</p>
                {[
                  { num: "1", title: "Fill in .env locally",  body: "Copy .env.example to .env. Set every REQUIRED variable above. Save the file." },
                  { num: "2", title: "Mirror to Cloudflare",  body: "Cloudflare Pages → Settings → Variables and Secrets → BUILD scope. Add every same key + value. Plaintext type. Save." },
                  { num: "3", title: "Retry deployment",      body: "Cloudflare → Deployments → latest → Retry deployment. Without this step PUBLIC_* vars stay on the previous build." },
                  { num: "4", title: "Verify in view-source", body: "Open the live site, view-source, search for the GTM/GA4 IDs you set. If you see them rendered in the HTML, tracking is wired." },
                  { num: "5", title: "Validate consent flow", body: "Click Reject All → confirm no GA4 / Ads cookies set. Click Accept All → confirm consent_state cookie + GTM dataLayer firing. Re-test in incognito." },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <span class="text-lg font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{s.num}</span>
                    <div class="flex-1">
                      <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                      <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                    </div>
                  </div>
                ))}
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Cookie Consent Setup</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Set up cookie consent + analytics for [CLIENT NAME] for go-live. Add the REQUIRED env vars to .env locally AND to Cloudflare Pages → Settings → Variables → Build scope: PUBLIC_GTM_ID (or PUBLIC_GTAG_ID if no GTM), PUBLIC_GA_MEASUREMENT_ID, GA4_MEASUREMENT_ID, GA4_API_SECRET, PUBLIC_SITE_NAME, SITE_ORIGIN. Do NOT hardcode tracking IDs in src/config/cookie-consent.config.ts — that file is env-driven. In the config file, only edit policyVersion (start at '1.0'), bannerTitle, bannerText, and the UK English button labels. Click Retry deployment in Cloudflare after saving env vars. Verify by view-sourcing the live site and grepping for the rendered GTM/GA4 IDs.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               19 — SWIPER CAROUSELS
          ─────────────────────────────────────────────────────── -->
          <article id="carousel" class="bg-white border-b border-[--color-border]">

            <header class="flex items-center justify-between px-8 py-5 border-b border-[--color-border]">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">26</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Swiper Carousels</h5>
                  <p class="text-sm text-[--color-caption] m-0">Swiper v11 via CDN. Brand colours overridden in global.css.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Carousel</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- TestimonialSlider component -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Prebuilt Component — TestimonialSlider</p>
                <div class="p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex flex-col gap-3">
                  <div class="flex items-center gap-2 flex-wrap">
                    <code class="code-label">src/components/TestimonialSlider.astro</code>
                  </div>
                  <p class="m-0 text-sm text-[--color-body]">Ready-made Swiper carousel for patient reviews and testimonials. Accepts a <code class="code-label">testimonials</code> array. Includes navigation arrows, pagination dots, responsive breakpoints, and optional autoplay.</p>
                  <pre class="code-block"><code>---
import TestimonialSlider from '../components/TestimonialSlider.astro';

const reviews = [
  &lbrace; name: "Sarah M.", role: "Physiotherapy patient", body: "Excellent care...", rating: 5 &rbrace;,
  &lbrace; name: "James K.", role: "Sports massage client",  body: "Fast recovery...", rating: 5 &rbrace;,
];
---

&lt;TestimonialSlider testimonials=&lbrace;reviews&rbrace; /&gt;</code></pre>
                </div>
              </div>

              <!-- Testimonial object shape -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Testimonial Object Shape</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { field: "name",   type: "string",    desc: "Patient or client full name" },
                    { field: "role",   type: "string",    desc: "Treatment type or patient context" },
                    { field: "body",   type: "string",    desc: "Review text (1–3 sentences)" },
                    { field: "rating", type: "number 1–5",desc: "Star rating — renders filled/empty stars" },
                  ].map(f => (
                    <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <code class="code-label shrink-0">{f.field}</code>
                      <code class="code-hex shrink-0">{f.type}</code>
                      <p class="m-0 text-xs text-[--color-body]">{f.desc}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Brand overrides -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Brand Colour Overrides — Already in global.css</p>
                <pre class="code-block"><code>/* global.css — Swiper brand overrides — do not remove */
.swiper-button-next,
.swiper-button-prev &lbrace;
  color: var(--color-primary) !important;
&rbrace;
.swiper-pagination-bullet-active &lbrace;
  background: var(--color-primary) !important;
&rbrace;</code></pre>
                <p class="m-0 text-sm text-[--color-body]">Swiper arrows and active pagination dots automatically use the client's brand colour. These overrides are global — they apply to every Swiper instance on the site.</p>
              </div>

              <!-- Breakpoint pattern -->
              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">Standard Breakpoints Used in TestimonialSlider</p>
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                  {[
                    { bp: "< 768px",   slides: "1" },
                    { bp: "768px+",    slides: "2" },
                    { bp: "1024px+",   slides: "3" },
                  ].map(b => (
                    <div class="flex flex-col gap-1 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] text-center">
                      <code class="code-hex">{b.bp}</code>
                      <p class="m-0 text-sm font-semibold text-[--color-headline]">{b.slides} slide{b.slides !== "1" ? "s" : ""}</p>
                    </div>
                  ))}
                </div>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Carousels</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Add a [testimonial / service / team] carousel to [SECTION]. Import TestimonialSlider from src/components/TestimonialSlider.astro and pass a testimonials array matching the shape: name (string), role (string), body (string), rating (number 1–5). Do not import Swiper CSS or JS manually — the component handles this. Do not override Swiper colours — they are set globally in global.css.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               27 — GOOGLE REVIEWS CAROUSEL
          ─────────────────────────────────────────────────────── -->
          <article id="reviews" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">27</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Google Reviews Carousel</h5>
                  <p class="text-sm text-[--color-caption] m-0">Free, manual Trustindex-style pattern. Global pre-footer trust section.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Global</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <!-- Plain English -->
              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Every clinic site has a Google Reviews carousel that sits between the booking CTA and the footer. It is loaded once globally via <code class="code-label">BaseLayout.astro</code> — you do not add it per page. Reviews are pasted manually into one TypeScript file. <strong>This is 100% free forever — no Trustindex account, no widget, no monthly fee.</strong>
                </p>
              </div>

              <!-- The Big Rule -->
              <div class="p-4 bg-[--color-primary]/8 border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm font-bold text-[--color-headline]">The Rule (no exceptions):</p>
                <ul class="list-none p-0 m-0 mt-2 flex flex-col gap-1">
                  <li class="text-sm text-[--color-body]">✓ 100% free — no Trustindex, no EmbedSocial, no paid widget</li>
                  <li class="text-sm text-[--color-body]">✓ Native Astro carousel — zero third-party JS, zero iframes</li>
                  <li class="text-sm text-[--color-body]">✓ Auto-filtered ≥4 stars · capped at 20 · sorted newest first</li>
                  <li class="text-sm text-[--color-body]">✓ Dynamic JSON-LD schema — built from the same data the carousel renders</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER fake or AI-generated reviews (UK ASA + Google ToS)</li>
                  <li class="text-sm text-[--color-body]">✗ NEVER pay for a Google Reviews widget</li>
                </ul>
              </div>

              <!-- Why this beats Trustindex -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Why this beats a paid widget</p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">Trustindex / EmbedSocial widget</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">Iframe embed, third-party CSS + JS (~150–250 KB), -10 to -20 PSI mobile, vendor lock-in, paid tiers from ~£9/month for full features.</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">This pattern (HMDG)</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">Static HTML, ~600 bytes deferred JS, 0–1 PSI delta, fully customisable, free forever, no signup. Same Trustindex behaviour without the middleman.</p>
                  </div>
                </div>
              </div>

              <!-- File layout -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">File layout</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">src/data/reviews.ts</code> <span class="text-xs text-[--color-caption]">— THE ONE FILE YOU EDIT. Reviews array + meta (average rating, total count, Maps URL).</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">src/components/ReviewsSection.astro</code> <span class="text-xs text-[--color-caption]">— Component (markup, ~600 byte deferred JS, CSS, dynamic JSON-LD). Do not edit per client.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">scripts/fetch-google-reviews.mjs</code> <span class="text-xs text-[--color-caption]">— OPTIONAL Google Places API fetcher. Only used if a clinic wants auto-refresh.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">.github/workflows/refresh-reviews.yml.example</code> <span class="text-xs text-[--color-caption]">— OPTIONAL weekly GitHub Actions cron. Rename to <code class="code-label">.yml</code> to activate.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">.claude/skills/googlereviews-carousel.md</code> <span class="text-xs text-[--color-caption]">— Full skill reference. Read this for complete pattern docs.</span></li>
                </ul>
              </div>

              <!-- Step-by-step manual update -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step-by-step — Manual update (the recommended path)</p>
                <p class="m-0 text-xs text-[--color-caption] italic">No API keys. No Google Cloud account. No automation. Just paste real reviews into a TypeScript array. This is what every HMDG clinic site uses by default.</p>
                {[
                  { num: "1", title: "Open the data file",
                    body: "Open src/data/reviews.ts. You will see a reviewsRaw array near the top with placeholder reviews. This is the only file you edit." },
                  { num: "2", title: "Visit the clinic's Google Reviews page",
                    body: "Go to the client's Google Maps profile (NOT Google Business Profile dashboard — the public Maps page). Click \"Reviews\" tab. Pick 8–15 of the best 4–5 star reviews from the last 6 months." },
                  { num: "3", title: "Paste each review into the array",
                    body: "Each entry needs five fields: name (string), rating (4 or 5), text (string — the review verbatim, do not edit it), relativeTime (e.g. \"2 weeks ago\"), date (ISO format YYYY-MM-DD — for sorting). Source is always 'google'." },
                  { num: "4", title: "Update the meta block",
                    body: "Update fallbackMeta with the clinic's real numbers: averageRating (e.g. 4.9), totalReviews (e.g. 247), totalReviewsDisplay (e.g. \"200+\"), and mapsUrl (the public Google Maps page for the clinic — used by the \"See all reviews on Google\" link)." },
                  { num: "5", title: "Commit + push",
                    body: "Cloudflare Pages rebuilds automatically. The site filters to ≥4 stars, sorts newest first, caps at 20, and re-emits the JSON-LD schema — all from your edits to that one file." },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <span class="text-lg font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{s.num}</span>
                    <div class="flex-1">
                      <p class="m-0 text-sm font-semibold text-[--color-headline]">{s.title}</p>
                      <p class="m-0 text-xs text-[--color-body] leading-relaxed">{s.body}</p>
                    </div>
                  </div>
                ))}
              </div>

              <!-- Review object shape -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Review object shape (paste this pattern, change the values)</p>
                <pre class="code-block"><code>{`{
  name:         'Sarah Mitchell',
  rating:       5,
  text:         "Lower back pain gone after 6 sessions. Genuinely life-changing care.",
  relativeTime: '2 weeks ago',
  date:         '2026-04-18',
  source:       'google',
},`}</code></pre>
                <p class="m-0 text-xs text-[--color-caption]">The display logic at the bottom of <code class="code-label">reviews.ts</code> filters ≥4 stars, sorts by date descending, and caps at 20. So you can safely paste 30+ reviews — only the freshest qualifying ones display.</p>
              </div>

              <!-- Dynamic JSON-LD schema -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Dynamic JSON-LD schema (built-in, automatic)</p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                    <strong>The component emits Schema.org structured data on every page</strong> — built dynamically from the same <code class="code-label">reviews</code> + <code class="code-label">reviewsMeta</code> exports that render the visible HTML. When you update reviews, the schema updates automatically. No manual maintenance, no second source of truth, no risk of schema drifting from what the page shows.
                  </p>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">LocalBusiness</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">Name, URL, @id anchor — declares the entity reviews belong to.</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">AggregateRating</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">ratingValue + reviewCount from <code class="code-label">reviewsMeta</code> — auto-formatted to 1 decimal.</p>
                  </div>
                  <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <p class="m-0 text-xs font-bold text-[--color-headline]">Review[]</p>
                    <p class="m-0 mt-1 text-xs text-[--color-body]">Author, rating, datePublished, reviewBody — one entry per visible review.</p>
                  </div>
                </div>
                <pre class="code-block"><code>{`// ReviewsSection.astro frontmatter
const reviewsLd = {
  '@context': 'https://schema.org',
  '@type':    'LocalBusiness',
  '@id':      \`\${siteUrl.replace(/\\/$/, '')}/#localbusiness\`,
  name:       siteName,
  url:        siteUrl,
  aggregateRating: {
    '@type':     'AggregateRating',
    ratingValue: Number(reviewsMeta.averageRating.toFixed(1)),
    reviewCount: reviewsMeta.totalReviews,
    bestRating:  5,
    worstRating: 1,
  },
  review: reviews.map(r => ({
    '@type': 'Review',
    author: { '@type': 'Person', name: r.name },
    reviewRating: {
      '@type':     'Rating',
      ratingValue: r.rating,
      bestRating:  5,
      worstRating: 1,
    },
    datePublished: r.date,
    reviewBody:    r.text,
  })),
};`}</code></pre>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-xs font-bold text-[--color-headline]">Realistic SEO impact</p>
                  <ul class="list-none p-0 m-0 mt-2 flex flex-col gap-1">
                    <li class="text-xs text-[--color-body]">✓ Reinforces local SEO trust signals (Google Knowledge Graph, AI search citations)</li>
                    <li class="text-xs text-[--color-body]">✓ Eligible for rich results when Google's policy on self-serving review stars shifts</li>
                    <li class="text-xs text-[--color-body]">✓ Zero downside — valid Schema.org, no penalty, no maintenance</li>
                    <li class="text-xs text-[--color-body]">✗ Will NOT magically rank you higher (schema is metadata, not a ranking factor)</li>
                    <li class="text-xs text-[--color-body]">✗ Star ratings will NOT appear in Google SERP (suppressed by 2019 self-serving policy)</li>
                  </ul>
                </div>
                <p class="m-0 text-xs text-[--color-caption]">Validate after each clinic launch via <code class="code-label">https://search.google.com/test/rich-results</code>.</p>
              </div>

              <!-- Customisation -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Per-client customisation points</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { token: "MAX_REVIEWS",  loc: "src/data/reviews.ts", desc: "Cap on reviews shown. Default 20." },
                    { token: "MIN_RATING",   loc: "src/data/reviews.ts", desc: "Filter floor. Default 4 — drops 1–3 star noise." },
                    { token: "<h2 id=\"reviews-headline\">", loc: "ReviewsSection.astro", desc: "Headline copy — default \"Trusted by Cheltenham Patients on Google\"." },
                    { token: "mapsUrl",      loc: "fallbackMeta in reviews.ts", desc: "Public Google Maps URL — powers the \"See all reviews\" link." },
                  ].map(c => (
                    <div class="flex flex-col gap-1 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <code class="code-label">{c.token}</code>
                      <code class="code-hex text-[10px]">{c.loc}</code>
                      <p class="m-0 text-xs text-[--color-body]">{c.desc}</p>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Optional auto-fetch -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Optional — auto-fetch from Google Places API</p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                    For clinics that want weekly auto-refresh: add <code class="code-label">GOOGLE_PLACES_API_KEY</code> + <code class="code-label">GOOGLE_PLACE_URL</code> to <code class="code-label">.env</code>, run <code class="code-label">npm run reviews:fetch</code>, commit <code class="code-label">src/data/reviews-cache.json</code>. Optionally rename <code class="code-label">refresh-reviews.yml.example</code> → <code class="code-label">.yml</code> for a Monday-06:00-UTC cron.
                  </p>
                  <p class="m-0 mt-2 text-xs text-[--color-caption]">
                    <strong>Cost:</strong> free in practice. Google Cloud's $200/month credit covers ~10 000 Place Details calls; weekly refresh = ~5 calls/month ≈ £0.07. Place API returns max 5 reviews per call, so most clinics combine auto-fetch (5 fresh) with manual paste (up to 15 older keepers).
                  </p>
                </div>
                <p class="m-0 text-xs text-[--color-caption]">Full setup: <code class="code-label">.claude/skills/googlereviews-carousel.md</code></p>
              </div>

              <!-- Per-client clone -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">When cloning to a new client</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Replace the reviewsRaw array in src/data/reviews.ts with the new clinic's real Google reviews (8–15 entries, all 4–5 star).",
                    "Update fallbackMeta — averageRating, totalReviews, totalReviewsDisplay, and mapsUrl.",
                    "Update the H2 in ReviewsSection.astro — change \"Cheltenham\" to the new clinic's location.",
                    "Update PUBLIC_SITE_NAME + Astro.site (astro.config.mjs) — these power the JSON-LD schema's name/url fields.",
                    "Validate after deploy at https://search.google.com/test/rich-results.",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

            </div>

            <div class="mx-8 mb-8 rounded-[--radius-card] overflow-hidden border border-[--color-border]">
              <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                <span class="caption-light">Claude AI Prompt — Update Google Reviews</span>
                <button class="copy-btn" type="button">Copy</button>
              </div>
              <div class="px-5 pt-4 pb-5">
                <p class="prompt-text prompt-code">Update the Google Reviews carousel for this clinic. Edit only src/data/reviews.ts. Replace the reviewsRaw array with [N] real Google reviews (paste each one verbatim — do not edit text, do not invent reviews). Each entry needs name, rating (4 or 5 only), text, relativeTime, date (YYYY-MM-DD), source: 'google'. Update fallbackMeta with the clinic's averageRating, totalReviews, totalReviewsDisplay, and mapsUrl (the public Google Maps page URL). Do not edit ReviewsSection.astro unless the H2 location keyword needs to change. Do not install Trustindex or any paid widget. Verify the JSON-LD schema renders correctly on the homepage view-source.</p>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               28 — CLIENT SETUP CHECKLIST
          ─────────────────────────────────────────────────────── -->
          <article id="setup" class="border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] bg-white flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">28</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Client Setup Checklist</h5>
                  <p class="text-sm text-[--color-caption] m-0">Two phases — what marketing must give you, then what dev does.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Setup</span>
            </header>

            <div class="px-8 py-6 bg-white flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> A clinic site needs information that ONLY the client / marketing team can give you (brand colours, analytics IDs, contact email, real address). Phase 1 is the briefing list to send them. Phase 2 is the build steps — start once Phase 1 is in your hands.
                </p>
              </div>

              <!-- ═════════════════════════════════════════════
                   PHASE 1 — MARKETING TEAM INPUTS
              ═════════════════════════════════════════════ -->
              <div class="border-t-2 border-[--color-primary] pt-5">
                <p class="text-[10px] font-bold text-white bg-[--color-primary] inline-block px-3 py-1 rounded-[--radius-badge] uppercase tracking-wider">Phase 1 — Required from Marketing Team</p>
                <p class="m-0 mt-3 text-sm text-[--color-caption]">Do not start dev work until every item below is in your hands. If any are missing, send the briefing prompt at the bottom of this section.</p>
              </div>

              <!-- M1 - Brand -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M1. Brand</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Primary brand colour (hex code, e.g. #DB003E)",
                    "Hover/darker shade of primary (hex)",
                    "Light accent tint (hex) — for section backgrounds",
                    "Logo files — .svg preferred, plus .png at 2× resolution",
                    "Favicon (.svg)",
                    "Confirm Inter Tight / Inter typography or specify alternative",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- M2 - Cookie & Analytics -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M2. Cookie Consent &amp; Analytics</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "GTM (Google Tag Manager) container ID, e.g. GTM-XXXXXXX — OR confirm we go straight to Cloudflare Zaraz",
                    "GA4 Measurement ID, e.g. G-XXXXXXXXXX (used twice — PUBLIC_ for browser + non-PUBLIC for server)",
                    "GA4 Measurement Protocol API Secret (GA4 → Admin → Data Streams → API secrets)",
                    "Banner copy (the consent banner text shown to visitors)",
                    "Cookie Policy URL + policy version number (e.g. v1.0)",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- M3 - Contact Form -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M3. Contact Form (Web3Forms)</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Web3Forms Pro account — login credentials, OR permission to create one and bill the client (~$8/month).",
                    "Primary recipient inbox — the email that should receive every contact form submission.",
                    "CC list — additional recipients copied on every submission, semicolon-separated. Example: client@x.co.uk; agency@hmdg.co.uk",
                    "Google reCAPTCHA v3 Site Key (from https://www.google.com/recaptcha/admin)",
                    "Google reCAPTCHA v3 Secret Key — pasted into Web3Forms dashboard, NEVER into code",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- M4 - NAP -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M4. NAP &amp; Structured Data</p>
                <p class="m-0 text-xs text-[--color-caption] italic">Used in the contact page, JSON-LD structured data, and footer.</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Full clinic name (exactly how it appears on Google Business Profile)",
                    "Phone number (display format AND tel: format, e.g. \"+44 (0) 1234 567 890\" + \"+441234567890\")",
                    "Email address (the public contact email)",
                    "Address line 1 (street)",
                    "Address line 2 (town + postcode, e.g. \"London, SW1A 1AA\")",
                    "Google Maps URL for the clinic",
                    "Opening hours per day (e.g. Mon–Fri 09:00–17:00, Sat closed, Sun closed)",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- M5 - Assets -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M5. Visual Assets</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Hero image — 1920×800 .webp (desktop). Required.",
                    "Hero image mobile — 800×800 .webp (optional, for the dual-preload pattern)",
                    "Social/OG image — 1200×630 .webp (used by Facebook/LinkedIn/iMessage previews)",
                    "Optional hero video source — ≤30s, 1080p+, any container/codec. Will be encoded to MP4 H.264 by us.",
                    "Service / treatment imagery (per page)",
                    "Team photos (if a Team page is included)",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- M6 - Legal -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">M6. Legal Page Content</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Privacy Policy — full text",
                    "Terms & Conditions — full text",
                    "Cookie Policy — full text (must match the consent banner copy from M2)",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- Marketing briefing prompt -->
              <div>
                <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                  <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                    <span class="caption-light">Claude AI Prompt — Marketing Briefing (copy + send to client)</span>
                    <button class="copy-btn" type="button">Copy</button>
                  </div>
                  <div class="px-5 pt-4 pb-5">
                    <p class="prompt-text prompt-code">Hi [CLIENT NAME] — to start your new clinic website we need the following from your marketing team. Most items are quick to gather. Send everything in one email or shared doc and we'll begin the build the same day. (1) BRAND: primary brand hex, hover hex, light accent hex, logo SVG and PNG, favicon, preferred fonts. (2) ANALYTICS: GTM container ID, GA4 Measurement ID, GA4 Measurement Protocol API Secret, consent banner copy, cookie policy URL and version. (3) CONTACT FORM: Web3Forms Pro account or permission to create one (~$8/mo billed to you), primary recipient inbox, CC list (semicolon-separated), Google reCAPTCHA v3 Site Key, Google reCAPTCHA v3 Secret Key. (4) BUSINESS: clinic legal name, phone (display + tel: format), public email, full address, Google Maps URL, opening hours per day. (5) ASSETS: hero image 1920×800 WebP, optional mobile hero 800×800, social image 1200×630, optional hero video source MP4. (6) LEGAL: Privacy Policy text, Terms &amp; Conditions text, Cookie Policy text. We do not start dev work until all six sections are in our hands.</p>
                  </div>
                </div>
              </div>

              <!-- ═════════════════════════════════════════════
                   PHASE 2 — DEV STEPS
              ═════════════════════════════════════════════ -->
              <div class="border-t-2 border-[--color-primary] pt-5 mt-2">
                <p class="text-[10px] font-bold text-white bg-[--color-headline] inline-block px-3 py-1 rounded-[--radius-badge] uppercase tracking-wider">Phase 2 — Dev Build Steps</p>
                <p class="m-0 mt-3 text-sm text-[--color-caption]">Run these steps in order. Each one builds on the previous.</p>
              </div>

              <!-- D1 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D1. Brand colours &amp; fonts · <code class="code-hex normal-case">src/styles/global.css @theme</code></p>
                {[
                  { token: "--color-primary",        action: "Set to client primary brand hex. Updates every button, link, and accent automatically." },
                  { token: "--color-primary-hover",  action: "Slightly darker or lighter shade of --color-primary. Hover state for interactive elements." },
                  { token: "--color-accent",         action: "Light tint of the brand colour. Section backgrounds + secondary fills." },
                  { token: "--font-heading",         action: "Change if client uses a different heading font. Inter Tight is the default." },
                  { token: "--font-body",            action: "Change if client uses a different body font. Inter is the default." },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <code class="code-label shrink-0">{s.token}</code>
                    <p class="m-0 text-sm text-[--color-body]">{s.action}</p>
                  </div>
                ))}
              </div>

              <!-- D2 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D2. Cookie consent · <code class="code-hex normal-case">src/config/cookie-consent.config.ts</code></p>
                <div class="p-4 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body]">Set <code class="code-label">gtmId</code>, <code class="code-label">gtagId</code>, <code class="code-label">bannerText</code>, and <code class="code-label">policyVersion</code>. Full field reference in section 25.</p>
                </div>
              </div>

              <!-- D3 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D3. Global components</p>
                {[
                  { file: "src/components/Header.astro",  action: "Update clinic name, logo, navigation links, phone, booking CTA." },
                  { file: "src/components/Footer.astro",  action: "Update clinic name, address, map embed, social links, legal page links." },
                  { file: "public/favicon.svg",           action: "Replace with the client's favicon. Keep .svg format." },
                ].map(s => (
                  <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                    <code class="code-label shrink-0">{s.file}</code>
                    <p class="m-0 text-sm text-[--color-body]">{s.action}</p>
                  </div>
                ))}
              </div>

              <!-- D4 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D4. Legal pages &amp; noindex rules</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { page: "/privacy-policy",      rule: "Add client content. Indexable at launch." },
                    { page: "/terms-conditions",    rule: "Add client content. Indexable at launch." },
                    { page: "/cookie-policy",       rule: "Add client content. Must match bannerText." },
                    { page: "/thank-you",           rule: "noindex={true} — already set in template." },
                    { page: "/thank-you-booking",   rule: "noindex={true} — already set in template." },
                    { page: "/contact",             rule: "Update per-client clinic data + JSON-LD hours." },
                  ].map(p => (
                    <div class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <div class="flex flex-col gap-0.5">
                        <code class="code-label">{p.page}</code>
                        <p class="m-0 text-xs text-[--color-body]">{p.rule}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              <!-- D5 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D5. Web3Forms wiring (NEW)</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Sign up / log in at web3forms.com. Upgrade to Pro (~$8/mo). Create a new form.",
                    "Web3Forms dashboard → Recipient Emails → primary client inbox. This is the PRIMARY recipient.",
                    "Web3Forms dashboard → Spam Protection → reCAPTCHA → paste the Secret Key from M3.",
                    "Copy the Form Access Key (UUID). This goes in PUBLIC_WEB3FORMS_KEY.",
                    "Set the four PUBLIC_* env vars locally and on Cloudflare Pages (see section 23).",
                    "Click Retry deployment in Cloudflare so the env vars bake into the build.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-6">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- D6 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D6. Performance assets (NEW)</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Drop the desktop hero at public/images/hero.webp (1920×800).",
                    "Optional: drop public/images/hero-mobile.webp (800×800) for the dual-preload pattern.",
                    "Drop public/og-image.webp (1200×630) for social sharing.",
                    "Optional: drop assets/videos/herovideo.mp4 (source). Run npm run optimise-video to encode → public/videos/hero-loop.mp4.",
                    "Run npm run resize-images to ensure no .webp exceeds 1920×1920.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-6">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- D7 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D7. Production origin (NEW)</p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body]">Set <code class="code-label">site:</code> in <code class="code-label">astro.config.mjs</code> to the production domain (e.g. <code class="code-label">'https://yourclinic.co.uk'</code>). This anchors canonical URLs, OG tags, and the auto-generated sitemap to the correct origin.</p>
                </div>
              </div>

              <!-- D8 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D8. Launch (NEW)</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Flip the noindex default in BaseLayout.astro from true → false. Permanent-noindex pages (thank-you, 404) keep their explicit noindex={true}.",
                    "Run a fresh PageSpeed audit on the production URL. Target: 90+ mobile.",
                    "Update Web3Forms dashboard → Domain → from localhost to the production custom domain.",
                    "Update Google reCAPTCHA admin → your site → Settings → add the production domain.",
                    "Update public/robots.txt → Sitemap line → swap pages.dev URL for production domain.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-6">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- D9 -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">D9. Post-launch (NEW)</p>
                <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                  <p class="m-0 text-sm text-[--color-body]">Once the custom domain is on Cloudflare proxy (orange cloud), migrate GTM → Cloudflare Zaraz. Eliminates 150–220 KB of browser JS. Full plan in <code class="code-label">.claude/skills/performance.md</code> Section 10.</p>
                </div>
              </div>

              <!-- API Routes -->
              <div class="border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest mb-3">API Routes Included</p>
                <div class="flex flex-col gap-2">
                  {[
                    { route: "/api/book-now",        file: "src/pages/api/book-now.ts",        desc: "Booking form submissions (server-side)." },
                    { route: "/api/booking-complete", file: "src/pages/api/booking-complete.ts", desc: "Post-booking confirmation flow + GA4 Measurement Protocol redundancy." },
                  ].map(r => (
                    <div class="flex items-start gap-4 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card] flex-wrap">
                      <code class="code-label shrink-0">{r.route}</code>
                      <code class="code-hex shrink-0">{r.file}</code>
                      <p class="m-0 text-sm text-[--color-body]">{r.desc}</p>
                    </div>
                  ))}
                </div>
              </div>

            </div>

            <div class="bg-white px-8 pb-8">
              <div class="rounded-[--radius-card] overflow-hidden border border-[--color-border]">
                <div class="px-5 py-3 flex items-center justify-between border-b border-[--color-border]">
                  <span class="caption-light">Claude AI Prompt — Run Phase 2</span>
                  <button class="copy-btn" type="button">Copy</button>
                </div>
                <div class="px-5 pt-4 pb-5">
                  <p class="prompt-text prompt-code">Set up this template for [CLIENT NAME] using the Phase 2 dev steps. D1: update --color-primary, --color-primary-hover, --color-accent in src/styles/global.css. D2: set gtmId, gtagId, bannerText, policyVersion in src/config/cookie-consent.config.ts. D3: update Header.astro, Footer.astro, favicon.svg. D4: add legal page content. D5: wire Web3Forms (4 PUBLIC_ env vars locally + Cloudflare Build scope, then Retry deployment). D6: drop hero.webp, hero-mobile.webp, og-image.webp, optionally encode hero video. D7: set site: in astro.config.mjs. D8: at launch, flip noindex default to false, PageSpeed 90+ mobile, update Web3Forms domain, update reCAPTCHA admin domains, update robots.txt sitemap URL. D9: post-launch GTM → Zaraz migration.</p>
                </div>
              </div>
            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               29 — DEPLOY TO CLOUDFLARE PAGES
          ─────────────────────────────────────────────────────── -->
          <article id="deploy" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">29</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Deploy to Cloudflare Pages</h5>
                  <p class="text-sm text-[--color-caption] m-0">Step-by-step. From a fresh clone to a live URL on Cloudflare Workers / Pages.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Deploy</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Cloudflare Pages is the host. It builds the Astro site from your GitHub repo and serves it from edge servers worldwide. Free for clinic-scale traffic. The steps below take ~15 minutes the first time, &lt;5 minutes for re-deploys.
                </p>
              </div>

              <!-- Pre-flight -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Pre-flight check</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "GitHub repo exists with the project pushed to main/master.",
                    "Cloudflare account exists (free tier is fine).",
                    "All 12 env vars from section 23 are ready.",
                    "Local npm run build passes cleanly.",
                  ].map(i => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <div class="icon-dot-offset mt-0.5"></div>
                      <p class="m-0 text-sm text-[--color-body]">{i}</p>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- Step 1 - Connect repo -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 1 — Connect the GitHub repo</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Sign in to Cloudflare dashboard at dash.cloudflare.com.",
                    "Left sidebar → Workers & Pages.",
                    'Click "Create application" → "Pages" tab → "Connect to Git".',
                    "Authorise Cloudflare for the GitHub organisation that owns the repo (one-time grant).",
                    "Select the repository from the list.",
                    'Click "Begin setup".',
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Step 2 - Build settings -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 2 — Build settings</p>
                <p class="m-0 text-xs text-[--color-caption]">Cloudflare auto-detects most of these for Astro projects. Verify they match exactly:</p>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {[
                    { label: "Framework preset", value: "Astro" },
                    { label: "Build command",    value: "npm run build" },
                    { label: "Build output",     value: "dist" },
                    { label: "Root directory",   value: "/" },
                    { label: "Node version",     value: "22.12.0 or higher" },
                    { label: "Production branch",value: "main (or master)" },
                  ].map(s => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <p class="m-0 text-[10px] font-bold text-[--color-caption] uppercase tracking-wide">{s.label}</p>
                      <code class="code-label text-[11px] mt-1 inline-block">{s.value}</code>
                    </div>
                  ))}
                </div>
              </div>

              <!-- Step 3 - Env vars -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 3 — Environment variables</p>
                <div class="p-3 bg-[--color-primary]/8 border-l-4 border-[--color-primary] rounded-[--radius-card]">
                  <p class="m-0 text-sm font-semibold text-[--color-headline]">Critical: choose BUILD scope, not Runtime.</p>
                  <p class="m-0 mt-1 text-xs text-[--color-body]">PUBLIC_* vars are baked into the HTML at build time. Setting them as Runtime does nothing.</p>
                </div>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Settings → Variables and Secrets → Add variable.",
                    "Add each of the 12 vars from section 23. Type: Plaintext.",
                    'Scope: tick "Build" (NOT "Runtime").',
                    "If a value contains a semicolon (PUBLIC_CLIENT_EMAIL), paste it directly — no escaping needed.",
                    "Save each one as you go.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Step 4 - Deploy -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 4 — First deploy</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    'Click "Save and Deploy". Cloudflare runs npm install + npm run build.',
                    "First build takes 2–4 minutes. Watch the live log.",
                    "Postinstall hook auto-runs scripts/patch-cloudflare-adapter.mjs (renames ASSETS → STATIC_ASSETS).",
                    "If you see error \"The name 'ASSETS' is reserved\" — the patch script didn't run. Check that scripts/ is committed and postinstall is in package.json.",
                    "On success, you get a *.pages.dev preview URL (e.g. yourclinic.pages.dev).",
                    "Open the preview URL and click around. Fill the contact form to verify Web3Forms delivery.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Step 5 - Custom domain -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 5 — Connect the custom domain</p>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Pages project → Custom domains → Set up a custom domain.",
                    "Enter the domain (e.g. yourclinic.co.uk). Cloudflare gives DNS instructions.",
                    "If the domain is registered with Cloudflare: nameservers route it automatically. If registered elsewhere: add a CNAME at the registrar pointing to your-project.pages.dev.",
                    "Wait for DNS propagation (5–30 minutes). Cloudflare provisions the SSL cert automatically.",
                    "Once connected, the domain shows green/active in the Pages dashboard.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Step 6 - Retry deployment dance -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 6 — Retry deployment after env-var changes</p>
                <div class="p-3 bg-[--color-primary]/8 border-l-4 border-[--color-primary] rounded-[--radius-card]">
                  <p class="m-0 text-sm font-semibold text-[--color-headline]">Cloudflare does NOT auto-rebuild when env vars change.</p>
                  <p class="m-0 mt-1 text-xs text-[--color-body]">After saving any new env var, you MUST trigger a redeploy or the new value is ignored.</p>
                </div>
                <ol class="list-none p-0 m-0 flex flex-col gap-2">
                  {[
                    "Pages project → Deployments tab.",
                    "Find the most recent successful deployment.",
                    'Click the "..." menu → "Retry deployment".',
                    "Cloudflare runs a fresh build with the current env vars baked in.",
                    "When green, the new values are live.",
                  ].map((step, i) => (
                    <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <span class="text-xs font-mono font-bold text-[--color-primary] shrink-0 w-8 text-center">{String(i + 1).padStart(2, "0")}</span>
                      <p class="m-0 text-sm text-[--color-body]">{step}</p>
                    </li>
                  ))}
                </ol>
              </div>

              <!-- Step 7 - Auto deploy on push -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Step 7 — Continuous deploys (automatic)</p>
                <p class="m-0 text-sm text-[--color-body]">Once connected, every push to the production branch (main/master) triggers an automatic build + deploy. Pull request branches get preview deploys at <code class="code-label">[branch].your-project.pages.dev</code>.</p>
              </div>

              <!-- Troubleshooting -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Troubleshooting</p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  {[
                    { problem: 'Build fails: "The name \'ASSETS\' is reserved in Pages projects"',
                      fix: "scripts/patch-cloudflare-adapter.mjs didn't run. Verify scripts/ is in the repo + package.json has \"postinstall\": \"node scripts/patch-cloudflare-adapter.mjs\"." },
                    { problem: 'Form submits but email subject is "Website Name"',
                      fix: "PUBLIC_SITE_NAME env var not set on Cloudflare, OR was set but didn't redeploy. Add it under Build scope and Retry deployment." },
                    { problem: "Form submits but no CC recipients receive email",
                      fix: 'PUBLIC_CLIENT_EMAIL has commas instead of semicolons. Web3Forms expects "a@x.co.uk; b@y.co.uk" exactly. Update + redeploy.' },
                    { problem: "reCAPTCHA fails on production but works locally",
                      fix: "Production domain not in reCAPTCHA admin's allowed domains list. https://www.google.com/recaptcha/admin → your site → Settings → add the domain." },
                    { problem: "Custom domain stuck in \"Provisioning\"",
                      fix: "DNS not propagated yet. Wait up to 30 minutes. If still stuck after an hour, verify the CNAME at the registrar." },
                    { problem: "404 on /thank-you/ even though the page exists",
                      fix: "Check public/_headers — the X-Robots-Tag rules use exact paths. The Astro build emits /thank-you/index.html so /thank-you/ works but /thank-you may not. Test both." },
                  ].map(t => (
                    <div class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                      <p class="m-0 text-xs font-bold text-[--color-headline]">{t.problem}</p>
                      <p class="m-0 mt-1 text-xs text-[--color-body] leading-relaxed">{t.fix}</p>
                    </div>
                  ))}
                </div>
              </div>

            </div>

          </article>


          <!-- ───────────────────────────────────────────────────
               30 — FALLBACK REDIRECTION
          ─────────────────────────────────────────────────────── -->
          <article id="fallback" class="bg-white border-b border-[--color-border]">

            <header class="flex items-start justify-between px-8 py-5 border-b border-[--color-border] flex-wrap gap-3">
              <div class="flex items-center gap-3">
                <span class="text-xs font-bold text-[--color-border] font-mono">30</span>
                <div class="flex flex-col gap-0.5">
                  <h5 class="m-0">Fallback Redirection</h5>
                  <p class="text-sm text-[--color-caption] m-0">Rank Math "Fallback Behaviour" equivalent — unmatched URLs 301 to the homepage on Cloudflare. Real 404s still surface on <code class="code-label">astro dev</code>.</p>
                </div>
              </div>
              <span class="text-xs font-semibold text-[--color-primary] bg-[--color-accent] px-3 py-1 rounded-[--radius-badge] uppercase tracking-wide self-start">Worker</span>
            </header>

            <div class="px-8 py-6 flex flex-col gap-6">

              <div class="p-4 bg-[--color-accent] border-l-4 border-[--color-primary] rounded-[--radius-card]">
                <p class="m-0 text-sm text-[--color-body] leading-relaxed">
                  <strong>Plain English:</strong> Ports the Rank Math "Fallback Behaviour → Redirect to Homepage" feature to Astro. Catches stray old URLs (forgotten WP permalinks, deep backlinks, crawler noise) at production and 301-redirects them to <code class="code-label">/</code> instead of returning 404 — preserving link equity during a WP → Astro migration.
                </p>
              </div>

              <!-- Behaviour table -->
              <div class="flex flex-col gap-3">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Behaviour</p>
                <div class="overflow-x-auto">
                  <table class="w-full border border-[--color-border] rounded-[--radius-card] overflow-hidden text-xs">
                    <thead class="bg-[--color-muted]">
                      <tr>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Environment</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Result for unmatched URL</th>
                        <th class="text-left px-3 py-2 font-bold text-[--color-headline]">Why</th>
                      </tr>
                    </thead>
                    <tbody>
                      <tr class="border-t border-[--color-border] bg-white">
                        <td class="px-3 py-2 align-top whitespace-nowrap"><code class="code-label text-[10px]">astro dev</code> (localhost)</td>
                        <td class="px-3 py-2 align-top text-[--color-body]">Real 404 — branded <code class="code-label">404.astro</code> renders</td>
                        <td class="px-3 py-2 align-top text-[--color-body] leading-relaxed">Surfaces missing routes, broken internal links, and typos during development.</td>
                      </tr>
                      <tr class="border-t border-[--color-border] bg-white">
                        <td class="px-3 py-2 align-top whitespace-nowrap">Cloudflare Pages (preview + production)</td>
                        <td class="px-3 py-2 align-top text-[--color-body]"><code class="code-label">301</code> → <code class="code-label">/</code></td>
                        <td class="px-3 py-2 align-top text-[--color-body] leading-relaxed">SEO migration safety — old WP URLs flow to the homepage instead of breaking.</td>
                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- Mechanism -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Mechanism — automatic gate, no manual toggle</p>
                <pre class="code-block"><code>{`// src/middleware.ts
const FALLBACK_REDIRECT_ENABLED = import.meta.env.PROD;
//                                ^^^^^^^^^^^^^^^^^^^
// false on \`astro dev\`, true on every \`astro build\` — no risk of
// forgetting to flip a manual switch on launch day.`}</code></pre>
              </div>

              <!-- Implementation files -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Implementation</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">src/middleware.ts</code> <span class="text-xs text-[--color-caption]">— intercepts 404 responses inside the Worker and rewrites to 301. Excludes <code class="code-label">/api/</code>, asset prefixes, robots.txt, sitemaps, favicon, manifest.</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">public/_redirects</code> <span class="text-xs text-[--color-caption]">— specific old → new mappings only. NEVER a wildcard (Cloudflare evaluates this before the Worker; a wildcard there loops the prerender simulator).</span></li>
                  <li class="p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]"><code class="code-label">.claude/prompts/fallback-redirect.md</code> <span class="text-xs text-[--color-caption]">— full architecture reference. Read for edge cases, exclusion rationale, and migration playbook.</span></li>
                </ul>
              </div>

              <!-- Per-client customisation -->
              <div class="flex flex-col gap-3 border-t border-[--color-border] pt-5">
                <p class="text-xs font-bold text-[--color-caption] uppercase tracking-widest m-0">Per-client customisation</p>
                <ul class="list-none p-0 m-0 flex flex-col gap-2">
                  <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="icon-dot-offset mt-0.5"></div>
                    <p class="m-0 text-sm text-[--color-body]">Map known WP migration URLs by adding specific old → new lines to <code class="code-label">public/_redirects</code> ABOVE the Rank Math comment block. The wildcard fallback in middleware only catches the long tail.</p>
                  </li>
                  <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="icon-dot-offset mt-0.5"></div>
                    <p class="m-0 text-sm text-[--color-body]">If the site uses public folders beyond <code class="code-label">/images/</code>, <code class="code-label">/videos/</code>, <code class="code-label">/fonts/</code>, <code class="code-label">/icons/</code>, <code class="code-label">/_astro/</code>, add the prefix to <code class="code-label">NEVER_FALLBACK_PREFIXES</code> in <code class="code-label">src/middleware.ts</code> so genuine 404s on those assets still surface as bugs.</p>
                  </li>
                  <li class="flex items-start gap-3 p-3 bg-[--color-muted] border border-[--color-border] rounded-[--radius-card]">
                    <div class="icon-dot-offset mt-0.5"></div>
                    <p class="m-0 text-sm text-[--color-body]">To test the fallback locally, run <code class="code-label">astro build</code> + <code class="code-label">astro preview</code> (uses PROD) instead of <code class="code-label">astro dev</code>.</p>
                  </li>
                </ul>
              </div>

            </div>

          </article>


        </main><!-- end articles -->


        <!-- FOOTER -->
        <footer class="ds-dark">

          <div class="px-[30px] py-8 flex-layout flex-layout--between gap-4">
            <span class="text-sm font-semibold text-white/70">HMDG Base Template</span>
          </div>

          <div class="border-t border-white/10">
            <div class="px-[30px] py-4 flex-layout flex-layout--between gap-4">
              <div class="flex-layout items-center gap-5 flex-wrap">
                <span class="text-sm text-white/60">© Copyright HMDG {new Date().getFullYear()}</span>
                <span class="text-sm text-white/60">
                  Designed &amp; Developed by
                  <a href="https://hmdg.co.uk/" target="_blank" rel="noopener noreferrer" class="font-semibold text-white/70 hover:text-white transition-colors">HMDG</a>
                </span>
              </div>
              <nav class="flex-layout items-center gap-5 flex-wrap" aria-label="Legal">
                <a href="/privacy-policy"   class="text-sm text-white/70 hover:text-white transition-colors">Privacy Policy</a>
                <a href="/terms-conditions" class="text-sm text-white/70 hover:text-white transition-colors">Terms &amp; Conditions</a>
                <a href="/cookie-policy"    class="text-sm text-white/70 hover:text-white transition-colors">Cookie Policy</a>
              </nav>
            </div>
          </div>

        </footer>


      </div><!-- end right panel -->
    </div><!-- end two-column layout -->


  <script>
    // ── Radius demos: set --demo-radius CSS variable from data-radius attribute ──
    (document.querySelectorAll('[data-radius]') as NodeListOf<HTMLElement>).forEach(el => {
      const v = el.getAttribute('data-radius');
      if (v) el.style.setProperty('--demo-radius', v);
    });

    // ── Colour swatches: apply data-hex as background via JS (dynamic values) ──
    (document.querySelectorAll('[data-hex]') as NodeListOf<HTMLElement>).forEach(el => {
      const v = el.getAttribute('data-hex');
      if (!v) return;
      el.style.background = v;
      // Add a subtle border so white/light swatches are always visible
      el.style.border = '1px solid rgba(0,0,0,0.08)';
    });

    // ── Copy-to-clipboard for AI Prompt boxes ──
    (document.querySelectorAll('.copy-btn') as NodeListOf<HTMLButtonElement>).forEach(btn => {
      btn.addEventListener('click', () => {
        let el: HTMLElement | null = btn.parentElement;
        let promptEl: Element | null = null;
        while (el && !promptEl) {
          promptEl = el.querySelector('.prompt-text');
          el = el.parentElement;
        }
        if (!promptEl) return;
        const text = (promptEl.textContent ?? '').trim();

        const onSuccess = () => {
          btn.textContent = '✓ Copied';
          btn.style.color = '#4ade80';
          btn.style.borderColor = 'rgba(74,222,128,0.35)';
          btn.style.background = 'rgba(74,222,128,0.08)';
          setTimeout(() => {
            btn.textContent = 'Copy';
            btn.style.color = '';
            btn.style.borderColor = '';
            btn.style.background = '';
          }, 2000);
        };

        if (navigator.clipboard?.writeText) {
          navigator.clipboard.writeText(text).then(onSuccess).catch(() => fallbackCopy(promptEl as Element, onSuccess));
        } else {
          fallbackCopy(promptEl, onSuccess);
        }
      });
    });

    function fallbackCopy(el: Element, onSuccess: () => void) {
      const range = document.createRange();
      range.selectNodeContents(el);
      const sel = window.getSelection();
      if (!sel) return;
      sel.removeAllRanges();
      sel.addRange(range);
      try { document.execCommand('copy'); onSuccess(); } catch { /* silent */ }
      sel.removeAllRanges();
    }

    // ── Sidebar active state via IntersectionObserver ──
    const sections = document.querySelectorAll('article[id]') as NodeListOf<HTMLElement>;
    const sidebarLinks = document.querySelectorAll('[data-nav]') as NodeListOf<HTMLElement>;

    if (sections.length && sidebarLinks.length) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              sidebarLinks.forEach(link => {
                const id = link.getAttribute('data-nav');
                link.classList.toggle('is-active', id === entry.target.id);
              });
            }
          });
        },
        { rootMargin: '-20% 0px -75% 0px', threshold: 0 }
      );
      sections.forEach(s => observer.observe(s));
    }

    /* Legacy Netlify Forms subject-rewrite block was removed.
       The contact form on this page (and across the site) is now Web3Forms,
       which handles subject + from_name personalisation in the canonical
       <ContactForm /> component itself. See .claude/skills/contactform.md. */
  </script>

  <!-- ── Cookie Consent ────────────────────────────────────────── -->
  <CookieConsent />

  </body>
</html>
